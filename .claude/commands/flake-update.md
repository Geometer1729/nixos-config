---
description: Update flake inputs in an isolated worktree, verify and deploy them, and write the update report.
---

Load and follow the `flake-update` skill in full.

Preserve its exhaustive per-input analysis and unmatched nixpkgs scan. Work only in the dated update worktree it specifies, deploy that worktree directly, and commit only after deployment and health checks finish.
