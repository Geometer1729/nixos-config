# Flake Update Report - 2026-09-20

## Summary

Branch: `update-09-20-26`, based on committed `c009360b127ae7619540ea9b04eccd8bc2461147`. Worktree: `/home/bbrian/Code/conf-update-09-20-26`. Evidence: `/tmp/flake-update/update-09-20-26`. All times are America/New_York (EDT).

**Deployment and required checks passed.** Eleven inputs advanced. Every changed input, all 465 stable nixpkgs commits, and both local overlays were reviewed. No configuration migration or overlay change was required. The update worktree was built, tested locally, and deployed directly to am, Balrog, and Torag. Balrog rebooted into Linux 6.18.52 with no failed units; all four local and Torag health recipes passed on verified expected generations. No host was skipped.

The first PrismLauncher build encountered an upstream-known, unchanged timing-sensitive test. The same derivation subsequently passed all 22 tests without disabling checks. New build/tooling observations are reconciled in `failures.md`. Am and Torag still need normal reboots to run the deployed kernel. Root usage on am is 97%, with about 27 GiB available.

Only `flake.lock`, the failure baseline, and this report belong to the update commit. Deployment and health verification preceded report finalization and committing. Merge remains subject to Brian's go-ahead.

## Input Updates

| Input | Old → new revision | Commits | Assessment |
|---|---|---:|---|
| `claude-code` | `bc83319` → `736ada3` | 9 | Claude Code 2.1.269 → 2.1.278; permission fixes, account skill/plugin synchronization, instruction discovery |
| `disko` | `ff8702b` → `725ea35` | 2 | Interactive LUKS password-confirmation fix; configured layouts do not use LUKS |
| `git-hooks` | `27555e2` → `59f4ca0` | 4 | Upstream output-evaluation optimization; consumed flake module and hooks unchanged |
| `home-manager` | `b1d1b60` → `ec17201` | 4 | Firefox-family custom config paths and Darwin migration documentation; those programs are disabled here |
| `nix-index-database` | `116ad1c` → `9ad7226` | 4 | Database refreshes; input is pinned but not wired into the configured nix-index installation |
| `nixpkgs` | `21a67dc` → `cf9d2fb` | 465 | Complete stable range; package, kernel, firmware, and build-infrastructure changes below |
| `nixpkgs-unstable` | `eaad089` → `20b1ddd` | 3,795 | Sole consumer is Brave Origin on am/Torag; 1.94.121 → 1.95.101 |
| `nixvim` | `68c2edd` → `0043fc9` | 1 | Locks/version metadata only; option and plugin implementations unchanged |
| `nur` | `3b0e0ec` → `bd3f5eb` | 497 | Registry updates; core/modules unchanged, indirect Firefox Color unchanged and inactive |
| `prismlauncher` | `4b35975` → `43a67fa` | 79 | Develop snapshot; resource/instance behavior and internal error-handling changes |
| `sops-nix` | `13616ff` → `7214124` | 5 | Optional systemd activation ordering fix; configured hosts use unchanged script activation |

There are no added or removed direct inputs. OpenCode's npm pin, both Linearis pins, and other unchanged inputs did not move. Nix printed an identical before/after notice for `xlibre-overlay`; a whole-node comparison confirms no lock change for it.

## Evidence Coverage

- The old lock is preserved. Programmatic comparison verified that `changelog.json` accounts for every changed top-level input; no manual file-input supplement was needed this time.
- Stable nixpkgs has `range_complete = true`: **465 unique commits**, **10 matched records**, and **455 unmatched subjects**. Matched/unmatched disjointness, exact union, and batch concatenation were verified.
- All **five batches** were reviewed: `batch-000` **100**, `batch-001` **100**, `batch-002` **100**, `batch-003` **100**, `batch-004` **55**. Every returned candidate received an endpoint/configuration relevance decision. No sampling or reduced batch count was used.
- All ten matched records and original/backport PR descriptions were inspected. Six records affect five selected packages. The excluded records concern the unused NixOS Syncthing module, stock PrismLauncher, and two Darwin-only Chrome changes. There are **zero `package: BREAKING` markers**.
- Package extraction found **404 unique names**. A separate explicit evaluation successfully enumerated system/Home Manager packages on all three hosts: am **292/214**, Balrog **140/142**, Torag **296/214**. No grep fallback or suppressed evaluation failure affected this run. The extractor's longstanding coverage limitations remain; the exhaustive scan covers indirect dependencies and infrastructure.
- API summaries are truncated for stable/unstable nixpkgs, NUR, and PrismLauncher. Local source ranges recovered complete counts; NUR's 497 commits and relevant 11-commit downstream registry range, Prism's 79 commits, and all other non-nixpkgs input commits were reviewed. Unstable analysis covers every actual consumer and its dependency range, rather than treating the first API page as complete.
- Unstable Brave's old/new closures each have 385 paths. Apart from Brave itself, all **384 dependencies** and **59 direct references** are identical; normalized wrappers are byte-identical. All four nixpkgs commits under the relevant Brave package path were reviewed.
- Detailed reviews are `review-*.md`; the coverage manifest is `review-coverage.json`. Three read-only reviewers could not write artifacts, so the parent preserved their complete returned summaries and coverage confirmations. No upstream checkout was placed inside the configuration worktree.

## Package And Closure Changes

Versions are for the changed am closure variants; coexisting copies are not implicitly upgraded. Torag also receives the stable Brave captive-portal browser update.

| Component | Old | New | Relevance |
|---|---|---|---|
| Linux / initrd | 6.18.50 | 6.18.52 | Default kernel on all installed hosts and installer; includes the intermediate .51 update |
| linux-firmware | 20260910 | 20260916 | Redistributable firmware enabled on all installed hosts |
| Brave Origin | 1.94.121 | 1.95.101 | Main browser from unstable on am/Torag |
| Stable Brave | 1.94.121 | 1.95.101 | Torag captive-portal browser |
| Ungoogled Chromium | 152.0.7977.82 | 153.0.8010.47 | Desktop browser; closes the previous report's Chromium 153 security-update gap |
| Google Chrome | 153.0.8010.36 | 153.0.8010.47 | Work and Yixin browser; 42 security fixes |
| Docker / Moby | 29.7.2 | 29.8.0 | Enabled on am/Torag; packaged Docker helper variants also advance |
| Docker's embedded runc / containerd | 1.4.3 / 2.3.3 | 1.5.1 / 2.3.4 | Separately pinned by Docker packaging; ordinary runc 1.4.3 remains unchanged |
| UPower | 1.91.3 | 1.91.4 | Enabled through Plasma; battery/backlight correctness and crash fixes |
| libmtp | 1.1.22 | 1.1.23 | Indirect Plasma/KIO device-transfer dependency |
| Claude Code | 2.1.269 | 2.1.278 | Direct flake package, work/personal wrappers |
| PrismLauncher | 2026-09-12 develop | 2026-09-20 develop | Configured develop build, still carrying the required Modrinth correction |
| Signal Desktop | 8.25.0 | 8.25.0 rebuilt | pnpm build-input change |
| Element Desktop | 1.12.26 | 1.12.26 rebuilt | pnpm build-input change |

Sops-install-secrets also rebuilds at the same displayed version. The pnpm change adds `pn` and `pnx` executable aliases; existing commands remain available. It changes Signal/Element build inputs and store paths, without adding pnpm to the runtime closure.

**Historical merge content was not counted as a fresh upgrade.** Batch 000's merge-first-parent diffs included Discord 1.0.156, Chrome's earlier 152 → 153 upgrade, and firmware 20260910. All were already present in the starting pin. Endpoint checks confirm identical Discord and ordinary runc store paths. Chrome and firmware have only the newer net changes listed above.

The am closure stays at **6,012 store paths**, replacing **153 paths with 153**. Size changes from **33,815,551,856 to 33,844,005,168 bytes**: **+28,453,312 bytes, approximately 27.1 MiB**. **No package base names were added or removed.** There are 22 added/removed versioned or generated names. Raw path lists and JSON remain in artifacts; `closure-diff.log`, `closure-summary.json`, and `batch-000-endpoint-closure-check.json` contain the comparisons.

## Relevance And Migration Review

### Action Required

- **No required configuration or overlay migration.** The successful builds, activation, deployment, and health checks use the updated lock without configuration workarounds.
- **Reboot GUI hosts when convenient.** Balrog completed the required reboot. Am runs 6.18.49 and Torag runs 6.18.50 until reboot; both have 6.18.52 deployed as the default system.

### Worth Reviewing

- **Browser security and web compatibility:** Ungoogled Chromium now receives Chromium 153, including the earlier exploited V8 CVE-2026-87491 fix and the .47 security update. Chromium 153 changes XML parsing to Rust in common non-XSLT paths, removes/deprecates Privacy Sandbox APIs, Related Website Sets and `document.requestStorageAccessFor`, and removes nonstandard `_current` navigation. No matching local policy requires migration; restart browsers and check real login/extension/webapp behavior.
- **Brave containers:** container hover names, temporary containers, icon fixes, Linux vertical-tab fixes, and Linux/macOS password import are relevant. Stored container keys, serialization, `--container=Work`, Wayland, VA-API, Default profile, and theme-extension arguments remain compatible. The initially flagged Cloud AP SSO restriction is **not applicable on Linux**: its Chromium source is compiled only for macOS/Windows/Android. This corrects the interim risk note.
- **Claude Code:** all releases 2.1.270–2.1.278 were inspected. Bash permission parsing is hardened, read-only Git prompting is corrected, plugin installation and subagent-output framing are improved, and sandboxed zsh failure reporting is fixed. Existing command hooks and account directories are unchanged. AGENTS.md is now discovered when CLAUDE.md is absent; this repository has both. Account skills/plugins now synchronize by default from claude.ai; with persisted work/personal account directories, consider declarative `syncClaudeAiSkills = false` and `syncClaudeAiPlugins = false` if remote synchronization is unwanted. Runtime auto-mode behavior depends on account/provider/mode; no such migration is declared here. The opaque binary and generic 2.1.272 notes limit deeper implementation review.
- **PrismLauncher:** per-action WideBar/sidebar hiding is removed, so nine old visibility settings become ignored; the inspected settings already show all actions. Managed CurseForge/Modrinth updates now ask for default-No confirmation, affecting the existing managed packs. Fixes cover Log4j timestamps/text, mod-folder filtering, resource issue refresh, context-menu searches, new-instance icons, network-dialog stacking, and unsafe version changes while an instance runs. The exception-to-`std::expected` refactor touches authentication, Java metadata, parsing, and platform APIs; these remain priority manual tests. Yixin's persisted state was not readable, so that user's runtime settings were not assessed.
- **Docker:** 29.8 supplies networking/Swarm synchronization fixes, rootless improvements, checkpoint-ID validation, metadata fixes and optional `--umask`. Reserved network names `container`/`container:` are now rejected; none is declared here. Older Docker 29.0/29.1 regression reports are not new regressions in this interval. The local daemon reports 29.8.0 and is active; project-specific network/build workflows remain manual checks. Foundry uses Podman and was independently healthy.
- **UPower:** composite keyboard-backlight support, impossible-charge/invalid-energy handling, multi-battery retention, and CLI/watch crash fixes apply through Plasma. Check Torag's battery and brightness behavior. GitLab's anti-bot block was worked around by reading upstream NEWS from Debian's original source tarball.
- **Capacity/build reliability:** am has about 27 GiB free. Prism's intermittent upstream test timeout is still a reproducibility follow-up, despite the successful unchanged retry.

### No Action Needed

- Disko changes only new-LUKS password confirmation. Installed layouts are GPT/LVM/Btrfs with no LUKS; the installer does not import Disko or perform partitioning.
- Home Manager's Firefox-family config-path and Darwin migration changes do not affect the disabled Firefox/Floorp/LibreWolf/Thunderbird programs or these Linux hosts.
- Git-hooks' imported module and all configured hooks are unchanged. Nixvim changes only dependency/version metadata; the root follows/source settings control the actual packages. Nix-index-database remains unused by the configured nix-index installation.
- NUR core/modules have no relevant change or repository removal. Stylix's indirect Firefox Color derivation is unchanged and inactive.
- Sops-nix's optional systemd ordering fix does not apply: all importing hosts evaluate `sops.useSystemdActivation = false`. Existing script activation and persistence paths are unchanged.
- The NixOS Syncthing `requires`/`requisite` fix is not selected by the Home Manager user-service configuration. Stock PrismLauncher 11.1 is not selected by the develop input.
- No applicable stable NixOS option removal, service migration, library API break, or new persistence requirement was found in the exhaustive scan.

### Breaking-Marker Disposition

The stable analyzer produced **zero `package: BREAKING` entries**. Potential removals/migrations found independently were still investigated:

| Candidate | Disposition |
|---|---|
| Electron 41 removal/insecurity | Neither endpoint's configured Element/Signal uses it; selected versions use Electron 42/43 |
| Firefox-family/Darwin config-path migration | Disabled programs and non-Linux behavior; no local migration |
| Prism per-action sidebar/WideBar controls removed | Applicable ignored settings, but inspected actions were already all visible; no replacement setting needed |
| Prism internal/CMake/header/error-handling removals | Source/build refactors, not additional removed local options; configured build and all tests passed |
| Chromium 153 web API removals | Browser behavior may affect external sites; no declared policy migration |
| Linux 7.3/LLVM 23/bcachefs candidates | Not selected by the configured kernel, toolchain, or filesystem paths |

## Overlay Review

| Overlay | Decision | Evidence and removal condition |
|---|---|---|
| `overlays/prismlauncher.nix` | Kept unchanged | Selected develop wrapper still omits libVLC, NSS and Wayland additions. Upstream still has the malformed combined Modrinth facet: unpatched request HTTP 400, corrected request HTTP 200. Built executable contains the corrected facet and no malformed facet. Remove the substitution only once upstream fixes it; retain runtime additions until supplied by the selected wrapper. |
| `overlays/vit.nix` | Kept unchanged | Nixpkgs still wires VIT 2.3.4/tasklib 2.5.1 to Taskwarrior 2, while this configuration uses Taskwarrior 3. VIT's search is still case-sensitive and issue #208 remains open. Existing comments state the independent removal conditions. |

## Build Findings And Baseline Reconciliation

Reconciliation uses the fixed starting `failures-before.md`, including both local and remote results.

- **New: Prism check timeout.** First build exited 1 at **08:46:22** because `ResourceFolderModelTest::test_removeResource()` line 161 exhausted its unchanged 10-second timer on the first JAR fixture addition. It had not reached removal/watch behavior. Upstream [PR #5912](https://github.com/PrismLauncher/PrismLauncher/pull/5912) already documents frequent timer failures. The same derivation passed **22/22 tests** on retry, finishing **08:51:07**; the class then took 0.08 seconds. Exact execution/I/O delay remains unknown. No test was disabled and no failing check was accepted as an exception. Evidence: `prism-build-failure.md`.
- **New: initrd SONAME warnings.** The builder warns about unavailable `libcrypt.so.1`/`.so.1.1` alternatives. ELF notes and source show it checks each alternative separately. Direct inspection of old am and all three new initrds confirms the same `libcrypt.so.2` symlink and byte-identical implementation; Balrog's new initrd boots successfully. No workaround is needed. Evidence: `initrd-warning-analysis.md`, `initrd-libcrypt-check.json`.
- **New warning signatures:** Prism Qt AutoUic renames a duplicate layout, and CMake ignores generic `CMAKE_EXPORT_NO_PACKAGE_REGISTRY`. Existing Java/Applet warnings remain. Builds/tests succeed; these are tracked with upstream packaging/UI follow-up.
- **Changed:** Nix contention also affected the store database, rather than only the prior user evaluation cache; affected commands ultimately passed. Am capacity is now 97% / 27 GiB free. Torag is 37% / 600 GiB free, with 279 MiB available on `/boot`.
- **Unchanged:** the two framework/platform evaluation warnings; PostgreSQL/X-server documentation collisions; gawk info-index warning; local/Torag Neovim warnings, including Torag's SSH-only clipboard warning; duplicate D-Bus/menu activation messages. Docker/PostgreSQL/UPower are active, Foundry is healthy, and no failed user units were found locally.
- **Tooling findings:** restart cancellation notifications did not accurately describe underlying command completion; automatic YAML LSP diagnostics lacked `yaml-language-server`. These are recorded with follow-up rather than hidden behind passing system checks.
- **No starting-baseline failure was declared resolved solely from a quiet journal.** Earlier hardware/resume, Foundry startup-race, KDE Connect and OpenCode-specific reproduction conditions remain unverified. Installer ISO build/boot, other advertised platforms, and GUI-host new-kernel boots remain explicit gaps.

## Deployment And Health Results

`just deploy` ran **09:05:40–09:19:06** from this worktree and exited 0. Formatting changed **0/101 files**; full flake checks, development-shell retention on both development hosts, and all three direct worktree switches succeeded.

| Host | Verified active/default system | Running kernel | Result |
|---|---|---|---|
| am | `/nix/store/iqccw0cj032y6pr7d196i8ajycd3dz5f-nixos-system-am-26.05.20260918.cf9d2fb` | 6.18.49 | Build/test/deploy and all four local health recipes passed; reboot pending |
| Balrog | `/nix/store/mb6z0brrs6jdawsy9giav3hd03v33yz3-nixos-system-balrog-26.05.20260918.cf9d2fb` | 6.18.52 | Deployment and required reboot passed; zero failed units |
| Torag | `/nix/store/w2i1207ilpybrvalh6q7l3byblqxz1vg-nixos-system-torag-26.05.20260918.cf9d2fb` | 6.18.50 | Deployment and all four remote health recipes passed; reboot pending |

Balrog's expected generation was checked immediately before reboot at **09:20:24**. At **09:20:48**, boot ID had changed from `81a609d5-13d6-45c9-bbef-85e005f4bbf0` to `dd754c8a-fbbc-4ced-b6a4-8d34a863d388`, Linux was 6.18.52, system state was `running`, and there were no failed units.

Local health recipes finished at **08:58**, and Torag's explicit-justfile recipes at **09:21**, with the expected current system verified before and after every recipe. Both hosts report two Syncthing peers and no GNOME packages; both remote-build invocations passed all 16 SSH/build/cache/signature assertions. Torag's temporary sleep inhibitor was released after checks at **09:22**. No deployment was run from Torag.

## Verification And Command Record

`W` means `/home/bbrian/Code/conf-update-09-20-26`; `A` means `/tmp/flake-update/update-09-20-26`. Repository commands used W after creation/approval. Source Git queries used the designated source repositories. Exact captured argv, working directories, timestamps and exits are in `A/command-records.json` and individual command JSON/log files; reviewer source/API/PR/release commands and results are retained in their review artifacts.

| Command / operation | Result |
|---|---|
| `date '+%m-%d-%y %Y-%m-%d %Z'`; `git worktree list --porcelain`; `git branch --list update-09-20-26`; initial `git status --short` | Date 09-20-26, EDT; dated branch/worktree absent; primary clean |
| `git worktree add --no-track -b update-09-20-26 "$W" HEAD` | Created from committed c009360 |
| `direnv allow "$W"`; session move | First attempt blocked by harness environment loading; same approval from trusted directory succeeded before edits; session moved |
| `flake-update --output-dir "$A" "$W"` | Exit 0; old lock preserved, 11 changed inputs, complete stable range |
| Python input-accounting, xlibre notice, package inventory, partition and batch audits | Passed; complete input and commit coverage; xlibre node unchanged |
| `ssh -o BatchMode=yes -o ConnectTimeout=10 {balrog,torag} ...` preflight | Balrog reachable; first Torag connection timed out. Brian woke Torag; subsequent preflight passed |
| `ssh torag keep-awake 24h`; final `ssh torag keep-awake off` | Both passed; temporary inhibitor cleaned up |
| Local date/hostname/current-system/kernel/failed-unit/filesystem probes; baseline snapshot | Captured old system and fixed starting failures baseline; no initial failed system/user units |
| `nh os build "$W" --out-link "$A/result"` | Original exit 1 from Prism test; unchanged resumed build passed. Original and resumed logs retained |
| `nix eval "$W#nixosConfigurations" --json --apply ...` package inventory | Passed for system and bbrian Home Manager packages on all three hosts |
| All-host expected-system evaluation | First foreground call timed out at 120s; subsequent and resumed calls passed; result link matches evaluated am output |
| `nix flake check "$W"` | Original and resumed full checks passed; deployment's full check also passed. All seven x86_64 checks built |
| Read-only per-input, matched, five-batch, and overlay reviews; targeted Prism failure escalation | Completed with all reported candidates dispositioned; no source fixes required |
| `flake-repo-checkout owner/repo old new "$A/repos/name"`; read-only Git/API/PR/release queries | Source evidence recovered outside W; limitations below and detailed command records in review artifacts |
| `nix path-info --recursive --json <old-system-or-result>`; `nix store diff-closures <old-system> "$A/result"`; Python closure summaries | Passed; raw closures retained. Implicit JSON-format deprecation noted; `nix path-info --json --json-format 2 "$A/result"` verified the explicit replacement without warnings |
| Targeted `readelf --dynamic/--notes`, source Git lookup/diff, and read-only zstd/cpio initrd inspection | Verified optional libcrypt alternatives and identical available implementation across all new host initrds |
| Diagnostic helper corrections | Broad source grep timed out; an unquoted glob and a JSON parse lacking warning-prefix stripping failed. Narrowed/quoted queries and corrected parsing succeeded |
| `nh os test "$W"` | Passed at 08:57; expected am generation activated |
| `just health`; `just vim-health`; `just gnome-check`; `just test-remote-builds` | All passed locally; exact generation checked around every recipe; warnings reconciled above |
| Local Docker-version, Podman Foundry-health, service-state and user-failed-unit queries | Docker 29.8.0, Foundry healthy, Docker/PostgreSQL/UPower active, no failed user units |
| SSH passwordless-elevation probes on all three hosts; local/remote `just --show` recipe comparison | Passed; all four Torag recipes identical to W |
| General NixOS regression search; targeted Docker 29.8 and Chromium 153 searches | Completed; primary release notes and reported-regression context assessed; manual checks below |
| `just deploy` from W | Exit 0; direct W deployment to am/Balrog/Torag and devshell retention passed |
| All-host current/default-system, kernel, boot-ID and failed-unit probes | All expected systems active/default, zero failed units |
| `ssh balrog sudo -n systemctl reboot`; bounded SSH reconnect; `systemctl is-system-running --wait`; post-boot probes | Passed with changed boot ID, expected system, Linux 6.18.52 and no failed units |
| `ssh torag just --justfile /home/bbrian/conf/justfile health` | Passed; no failed units, two Syncthing peers, existing journal warnings |
| `ssh torag just --justfile /home/bbrian/conf/justfile vim-health` | Passed with existing warnings; full checkhealth text retained |
| `ssh torag just --justfile /home/bbrian/conf/justfile gnome-check` | Passed; no GNOME packages |
| `ssh torag just --justfile /home/bbrian/conf/justfile test-remote-builds` | Passed all 16 assertions after Balrog reboot |
| `ssh torag df -h /boot` | Passed; 279 MiB free |
| Targeted Brave rewrite and Chromium platform-auth build-condition inspection | Cloud AP candidate excluded on Linux; interim applicability claim corrected |
| Attempt to copy original server-local flake-update shell log | Source log no longer existed after restart; preserved lock/changelog/range evidence and completed-command notification establish results; update was not rerun |
| Final baseline/report edits, Git status/diff/log inspection, whitespace check, selective staging and update commit | Performed after deployment and reachable checks; only update files included; commit identity supplied in final handoff |

### Tool Limitations

The server restart reported three commands cancelled, but durable wrapper records show they continued: original build exited 1 at 08:46:22, original full flake check passed at 08:51:11, and expected-system evaluation passed at 08:48:03. Resumption initially followed the cancellation notices; subsequent reconciliation uses actual records. The separate first 120-second evaluation really was interrupted. A later transport interruption during finalization applied no patch; completed checks were retained.

Some reviewer all-host evaluations timed out; targeted evaluations and the mandatory full checks supplied coverage. Upstream `.envrc` restrictions and the absent YAML language-server executable limited automatic LSP diagnostics, not read-only source review. UPower GitLab/alternate source pages were anti-bot blocked; original-tarball NEWS recovered the release evidence. These limitations do not reduce the five-batch scan or substitute API samples for complete relevant ranges.

## Follow-Up And Manual Tests

- Reboot am/Torag for Linux 6.18.52 and firmware, then test displays/GPU acceleration, Wi-Fi/Bluetooth, audio, suspend/resume and Torag battery/brightness.
- Restart browsers; verify normal/Work container separation, persisted profiles, extensions, work login, SVG/XML/webapps and portal-based screen sharing. Cloud AP SSO specifically is not a Linux concern.
- Decide whether Claude account skill/plugin synchronization fits the declarative setup. Test work/personal wrappers, allowed/denied Git rules, hooks, session resume, plugin behavior and sandboxed zsh error handling where used.
- In Prism, test managed-update cancel/confirm, combined Modrinth environment filters, resource/mod folders, Microsoft authentication, Java detection, current Log4j timestamps and network retry dialogs; verify native Wayland and WATERMeDIA/libVLC. Reproduce the upstream test timeout in a retained disposable build tree before proposing a fix.
- Smoke-test Signal/Element after their unchanged-version rebuilds, MTP device transfer, normal Docker builds, bind-mounted file permissions, container DNS and published ports.
- Review am disk capacity and the recorded OpenCode tool-environment/restart recovery issues. Preserve the outstanding installer/other-platform and intermittent-runtime verification gaps.

## Upstream References

- [NixOS 26.05 release notes](https://nixos.org/manual/nixos/stable/release-notes)
- [Chrome 153 release notes](https://developer.chrome.com/release-notes/153) and [153.0.8010.47 security release](https://chromereleases.googleblog.com/2026/09/stable-channel-update-for-desktop_0541751186.html)
- [Docker Engine 29 release notes](https://docs.docker.com/engine/release-notes/29/)
- [Claude Code changelog](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)
- [PrismLauncher timeout history, PR #5912](https://github.com/PrismLauncher/PrismLauncher/pull/5912) and [Modrinth migration, PR #6045](https://github.com/PrismLauncher/PrismLauncher/pull/6045)
- [UPower 1.91.4 NEWS](https://gitlab.freedesktop.org/upower/upower/-/blob/v1.91.4/NEWS)
- [Chromium platform-auth build conditions](https://github.com/chromium/chromium/blob/153.0.8010.47/chrome/browser/enterprise/BUILD.gn#L631-L637)
