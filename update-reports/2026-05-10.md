# Flake Update Report - 2026-05-10

No `nixpkgs` update this run.

## Build Status
- `nh os build` - OK
- `nix flake check` - All checks passed
- `nh os test` - Activated successfully
- `just health` - Local host reports Syncthing peer `3MB5CXC` disconnected; likely because `torag` is unreachable
- `just vim-health` - `is not in runtimepath` error no longer present; only `biber is not executable!` remains on `am`
- `just gnome-check` - Clean
- `just deploy` - Built `am` and `torag`; failed at `ssh torag git -C ~/conf pull` because `torag` timed out
- `just test-remote-builds` - Failed because `torag` is unreachable
- Torag host health checks - Not run; host unreachable

## Config Changes Required

### Fixed: Blueman applet user unit conflict
Home Manager/NixOS integration was generating a broken `blueman-applet.service` override with a duplicate `ExecStart`, so Bluetooth tray startup failed after activation.

Fix applied:
- `modules/nixos/bt.nix` - set `services.blueman.withApplet = false;`
- `modules/home/hyprland/hyprland.nix` - start `blueman-applet` from Hyprland `exec-once`

Verification:
- `systemctl --user start blueman-applet.service` now succeeds
- `systemctl --user status blueman-applet.service` shows the applet running

## Updated Inputs

| Input | Commits | Notes |
|-------|---------|-------|
| claude-code | 6 | Claude Code 2.1.132 -> 2.1.138 via package bumps; CI-only changes otherwise |
| firefox-addons | 4 | Automatic addon index updates; repo does not currently install packages from this set |
| home-manager | 14 | Mostly modular-services docs/tests/renames; no direct config impact found |
| nix-index-database | 5 | Generated database refresh; input is declared but not actively wired in repo modules |
| nur | 140 | Bot churn; only relevant risk was import/eval breakage, not seen here |
| prismlauncher | 32 | `develop` branch bump; includes app changes and an ATL path traversal fix |

## nixpkgs Package Changes Affecting Config

None. `nixpkgs` did not change, so there are no package-level or NixOS module-level upstream changes to review in this update.

## Input Relevance Summary

### No Action Needed
- `firefox-addons` - `modules/home/firefox.nix` references the package set, but the list is empty and actual forced extensions come from AMO URLs.
- `nix-index-database` - declared in `flake.nix`, but no active module usage found.
- `home-manager` modular-services rename commits - no `modular-services`, `home-services`, or `system-services` usage in repo.

### Worth Reviewing
- `claude-code` - actual installed package changed; no schema/module breakage indicated, but CLI behavior may differ.
- `prismlauncher` - repo tracks `develop` intentionally, so runtime smoke testing matters more than usual.
- `nur` - imported on the NixOS side, so import/eval regressions would matter even without direct package usage.

### Action Required
- Bluetooth tray startup was broken after activation and has been fixed in repo config.

## BREAKING Flags
- Home Manager changelog flagged `modular-services: rename home-manager -> Home Manager`
- Home Manager changelog flagged `modular-services: rename system-services -> home-services`

These do not affect this repo because those names are not referenced anywhere in configuration.

## Closure Changes
- **ADDED**: no meaningful package additions on `am`; only config/unit changes from the Bluetooth fix
- **REMOVED**: broken `unit-blueman-applet.service` wrapper from the activated local system
- **SIZE**: local system stayed effectively flat after the input update; torag build output showed a larger unrelated closure reshuffle, but deployment did not complete there

## Health Check Changes from Baseline
- **Resolved on am**: Neovim `tree-sitter-cli` error is gone
- **Resolved on am**: Neovim `is not in runtimepath` error is gone
- **Resolved on am**: stale provider/version warnings no longer appear in `just vim-health`
- **Still present on am**: `biber is not executable!`
- **New local issue**: `just health` reports Syncthing peer `3MB5CXC` disconnected
- `failures.md` updated for the verified `am` vim-health baseline only; `torag` remains unverified because the host is offline

## Notable Upstream Details

### PrismLauncher
- This repo uses `inputs.prismlauncher.overlays.default` and installs `pkgs.prismlauncher`, so the update is real user-facing risk.
- Upstream commits include an ATL path traversal fix and several auth/download/UI fixes.
- Because this input tracks `develop`, successful build is not enough; launcher smoke tests matter.

### Home Manager
- The modular-services rename commits are upstream churn, but they do not map to any options or code used here.
- `mako: allow null package` and `direnv: avoid duplicate fish hook` are irrelevant to current config usage.

## Notes / Follow-up
- `torag` is currently unreachable over SSH, which blocked `just deploy`, torag-side health checks, and remote-build verification.
- The Bluetooth applet fix is verified by manual user-service start, but Hyprland `exec-once` auto-start should still be confirmed on the next graphical login.
- Syncthing health may recover automatically once `torag` is back online if that disconnected peer is the remote host.

## Suggested Tests
1. Log out/in on `am` and verify the Bluetooth tray applet appears automatically.
2. Open Blueman and pair/connect a device to confirm the applet fix is not just a clean service start.
3. Launch PrismLauncher and verify normal startup.
4. In PrismLauncher, test Microsoft/Xbox auth and a Modrinth/modpack flow, since this repo tracks `develop` for the new auth path.
5. Run `claude --version` and one normal Claude Code workflow to catch any runtime behavior changes from 2.1.138.
6. Bring `torag` online, then rerun `just deploy`, `just health`, `just vim-health`, `just gnome-check`, and `just test-remote-builds`.
