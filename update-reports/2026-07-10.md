# Flake Update Report - 2026-07-10

Updated `nixpkgs` from `a50de1b7d8a586adc18d2395c19de7d6058e6030` to `0ad6f47ea4fe188f4bc8f0380f93ae8523337c6c` (nixos-26.05, 171 commits).

## Build Status

- `flake-update ~/conf` - OK; 9 inputs updated, changelogs generated under `/tmp/flake-update/`
- `nh os build` - OK; built `/nix/store/62n7xjzzavj4shiplbr1lz5prf3yh0nq-nixos-system-am-26.05.20260707.0ad6f47`
- `nix flake check` - OK; all checks passed. Warnings were pre-existing or non-blocking: unknown `nixos-unified` flake output, `system` alias warning, x86_64-darwin deprecation, and `programs.ssh.matchBlocks` deprecation.
- `nh os test` - OK; activated `/nix/store/62n7xjzzavj4shiplbr1lz5prf3yh0nq-nixos-system-am-26.05.20260707.0ad6f47`
- `systemctl --failed --no-pager` - clean after activation
- `just health` - baseline-only failure: Syncthing peer `3MB5CXC` disconnected. dbus-broker duplicate-name journal noise also matches baseline.
- `just vim-health` - baseline-only: runtimepath error, Nvim 0.12.4 availability notice, large LSP log, three unknown YAML filetypes, and missing `biber`
- `just gnome-check` - clean
- `just deploy` - not run: torag SSH timed out; user chose to skip torag-dependent checks for this run
- `just test-remote-builds` - not run: torag unreachable
- torag-side `just health`, `just vim-health`, `just gnome-check` - not run: torag unreachable

`nh os build` warnings to keep in mind:

- `programs.ssh.matchBlocks` is still deprecated in `modules/home/work.nix` and `modules/home/ssh.nix`; this is pre-existing.
- `pkgs.buildEnv` still reports a colliding `postgres` binary between `postgresql-and-plugins-18.4` and `postgresql-18.4`.
- initrd build printed missing `libcrypt.so.1` / `libcrypt.so.1.1` dependency warnings for `systemd-260.2` shared library; build and activation still succeeded.

## Updated Inputs

| Input | Old | New | Commits | Notes |
|-------|-----|-----|---------|-------|
| `claude-code` | `9b05ec91` | `d120e1bf` | 5 | Claude Code package bumps 2.1.202 through 2.1.206; config uses this as `programs.claude-code.package` |
| `firefox-addons` | `3a074d91` | `dd7e87fe` | 7 | Automatic Firefox/Thunderbird addon index updates; current `modules/home/firefox.nix` package list is empty, so no effective config impact |
| `meridian` | `d72b8c38` | `661276d7` | 15 | Meridian 1.45.1 plus passthrough/tool-loop fixes; relevant to `services.meridian` and the opencode plugin |
| `nix-darwin` | `a1fa429e` | `d5bd9cd7` | 4 | Manual/website/nixpkgs bump fixes; no Darwin configs in this repo |
| `nix-index-database` | `fab14c7b` | `96d6de10` | 2 | Docs/CI updates only. Input appears declared but not imported; `programs.nix-index` uses the normal module path |
| `nixpkgs` | `a50de1b7` | `0ad6f47e` | 171 | See nixpkgs analysis below |
| `nixpkgs-unstable` | `d4079514` | `0bb7ec54` | 3432 | Only used for `unstable.opencode`; closure shows opencode 1.17.12 -> 1.17.15 |
| `nur` | `fad6ed06` | `bea54b77` | 273 | NUR module imports only; no direct `pkgs.nur` package usage found |
| `stylix` | `8d56f0f6` | `e602ad04` | 2 | Maintainer metadata changes only; no option changes found |

## nixpkgs Changes Affecting Config

Combined results from the package-matched changelog (5 entries) and the unmatched commit scan:

| Package / Area | Change | Config Relevance | Action |
|----------------|--------|------------------|--------|
| `syncthing` | 2.0.15 -> 2.1.0 -> 2.1.1 | `services.syncthing` health is checked by `just health`; peer disconnect is baseline | No config action. 2.1 adds optional GUI grouping, CONNECT proxy support, `blockIndexing`, and GUI session cookie controls; 2.1.1 is bugfix-only |
| `syncthing` | Add zainkergaye as maintainer | Metadata only | None |
| `google-chrome` | 149.0.7827.201 -> 150.0.7871.47, Darwin only in package-matched nixpkgs window | Work browser config references Chrome; this specific commit is Darwin-only | None for NixOS |
| `ungoogled-chromium` | 149.0.7827.200-1 -> 150.0.7871.46-1 | Daily browser and webapp launcher | None; restart browser/webapps after update. Chrome release channel also has 150.0.7871.100 for Linux shortly after this nixpkgs rev, so expect another browser bump soon |
| `linux` / initrd | Kernel closure changed 6.18.37 -> 6.18.38 | Boot/initrd, impermanence rollback, hardware, Docker/containers, Hyprland display stack | No config action. Verify next reboot and display/session behavior |
| BusyBox | Backported tar symlink bug fix | Infrastructure-level; potentially relevant to initrd/rescue/minimal tooling | No config action |
| `gh` | 2.93.0 -> 2.96.0 | Installed and used by git credential helper/scripts/services | No config action. Smoke-test `gh auth status`, GitHub credential use, and `gh`-based scripts if issues appear |
| `nh` / `nh-unwrapped` | 4.3.2 -> 4.4.0 | Primary rebuild workflow; `NH_FLAKE` set in config | Build/test worked. Note breaking change only affects `nh search` CLI shape (`nh search --options` -> `nh search options`) |
| `opencode` from `nixpkgs-unstable` | 1.17.12 -> 1.17.15 | `modules/home/opencode.nix` installs `unstable.opencode` and routes through Meridian | No config action. Smoke-test opencode + Meridian workflow |
| `nvidia-x11` | 580.142 -> 580.173.02 backport | Display stack only if undeclared NVIDIA hardware exists | No action; no NVIDIA config found |
| `python311.pkgs.pip` | Eval fix | Potentially broad eval fix, no direct `python311` use found | None |

## BREAKING Flags

- `fix: remove MultiEdit from BLOCKED_BUILTIN_TOOLS` - not a breaking removal for this config; likely improves Meridian/opencode tool support.
- `linux_7_0: remove` and `[Backport staging-nixos-26.05] linux_7_0: remove (#538131)` - not relevant; this config does not select Linux 7.0.
- `lms: 3.74.0 -> 3.78.0; lms:Remove unused dependencies libICE and libSM` - not relevant; `lms` is not in the config package set.
- `treewide: remove danth from some targets (#2396)` - Stylix maintainer metadata only.
- `nh 4.4.0` has a real upstream breaking change for `nh search`; this config and the update workflow use `nh os build` / `nh os test`, which still worked.

## Overlay Review

- `overlays/prismlauncher.nix` - kept. Current nixpkgs `pkgs/by-name/pr/prismlauncher/package.nix` still exposes `additionalLibs`; the overlay remains the right place to add `libvlc` and `nss` for the installed mod set. Existing comments explain why it exists and when to remove it.
- `overlays/vit.nix` - kept. Current nixpkgs `vit` and `python3Packages.tasklib` still consume `taskwarrior2`; the overlay still maps them to `taskwarrior3`. The case-insensitive search patch remains local behavior not present upstream. Existing comments explain why it exists and when to remove it.

## Closure Changes

`nh os build` diff from `/run/current-system` to the new build:

- Size: 25.0 GiB -> 25.1 GiB (+64.6 MiB)
- Added packages: none reported by `nh`
- Removed packages: none reported by `nh`
- Changed packages: `claude-code` 2.1.201 -> 2.1.206, `gh` 2.93.0 -> 2.96.0, `linux` / `initrd-linux` 6.18.37 -> 6.18.38, `meridian` 1.45.0 -> 1.45.1, `nh` / `nh-unwrapped` 4.3.2 -> 4.4.0, `opencode` 1.17.12 -> 1.17.15, `syncthing` 2.0.15 -> 2.1.1

## Health Check Changes from Baseline

- No new local failures found.
- `failures.md` was not changed.
- `just health` still fails only because Syncthing peer `3MB5CXC` is disconnected, already in the baseline.
- `just vim-health` output matches the documented baseline.
- `just gnome-check` remains clean.
- torag baseline could not be revalidated because SSH timed out.

## Tool / Skill Limitations

- `nixpkgs-changelog.json` says `total_commits_scanned: 199`, while the GitHub compare API reports 171 commits for the same old/new nixpkgs revs. The skill should use compare API `total_commits` or count commit objects rather than a secondary scan count.
- `gh api ... -q '.commits[].commit.message'` produces multi-line commit messages; using `wc -l` on that file overcounts commits. The skill should suggest extracting `sha + first line` or JSON objects for batching.
- `nix eval` examples that use `builtins.getFlake "git+file:///home/bbrian/conf"` need `--impure`; without it, Nix rejects the unlocked local flake reference.
- The script's keyword-based “Potential Breaking Changes” section still flags false positives from ordinary words like `remove`; relevance filtering by input usage would reduce noise.

## Warnings / Follow-up

1. Bring torag online later and run `just deploy`, `just test-remote-builds`, and torag-side `just health`, `just vim-health`, `just gnome-check`.
2. Reboot am when convenient to validate kernel 6.18.38, initrd, impermanence rollback, display/session behavior, and Docker/Podman workloads.
3. Smoke-test `opencode` through Meridian; this update changes Meridian passthrough/tool-loop behavior and opencode also bumped.
4. Smoke-test Syncthing after torag is reachable; 2.1 adds optional config features but should migrate automatically.
5. If you use `nh search`, update muscle memory/scripts for the 4.4.0 CLI restructure.
6. Migrate `programs.ssh.matchBlocks` to `programs.ssh.settings` in `modules/home/ssh.nix` and `modules/home/work.nix`; the deprecation remains.
