# Flake Update Report - 2026-09-12

## Summary

Update branch: `update-09-12-26`, based on committed `dd50a7b`. Worktree: `/home/bbrian/Code/conf-update-09-12-26`. Evidence: `/tmp/flake-update/update-09-12-26`. All times in this report are America/New_York (EDT).

The input update and local verification ran September 12; deployment recovery and final remote verification continued September 13, retaining the original branch/report date.

Ten Git inputs advanced. A required Linearis npm pin alignment brings the total to eleven changed direct inputs. The update also includes a temporary PrismLauncher Modrinth filter correction, approved by Brian. After reviewing a V1/V2 database-selection conflict, Brian requested removing OpenCode V1 and committing to V2. Both existing overlays remain necessary; VIT's comments now distinguish the independent removal conditions.

**Update, merge, and post-merge local verification complete.** The final V2-only update built and activated, all x86_64-linux flake checks passed, and all four local and remote health recipes passed on verified expected generations. Final `just deploy` succeeded for all three hosts, and Balrog's required final-system reboot passed on September 13. Torag's initial `/boot` ENOSPC failure was resolved with Brian's approved legacy-file cleanup and declarative boot-generation limit. The skill and permissions now use the correct explicit remote justfile. Earlier local attempts encountered concurrent primary-worktree activations and a transient Foundry startup-health failure; final results were collected after Brian paused the other task. At Brian's subsequent request, the update was committed and merged into `master`; the combined local configuration also passed build, activation, full flake checks, and all four health recipes. See the post-merge section for that distinct configuration and its results.

## Input Updates

| Input | Revision/version | Commits | Assessment |
|---|---|---:|---|
| `claude-code` | `c14a457` → `bc83319` | 6 | Claude Code 2.1.263 → 2.1.269; permission, session, plugin, and terminal fixes |
| `firefox-addons` | `2554c0e` → `811170b` | 8 | Automated index refreshes; no directly selected add-on package changes |
| `home-manager` | `fd0956c` → `b1d1b60` | 1 | Upstream lock-only nixpkgs bump; follows this configuration's nixpkgs |
| `linearis` | `6e2973f` → `dfe34c2` | 4 | Private file-download authorization fixes; version 2026.9.0-next.1 |
| `linearis-npm` | 2026.8.0 → 2026.9.0-next.1 | N/A | Registry metadata aligned with the source; same four source commits |
| `nixos-unified` | `d2818c3` → `c411aaf` | 3 | Replaces deprecated `stdenv.isDarwin` with `stdenv.hostPlatform.isDarwin` |
| `nixpkgs` | `6713828` → `21a67dc` | 419 | Stable package, kernel, and firmware update |
| `nixpkgs-unstable` | `c043004` → `eaad089` | 4,308 | Brave Origin remains the sole direct consumer after the requested V1 removal |
| `nur` | `cdbdbb6` → `3b0e0ec` | 388 | Registry pin churn; Firefox Color 2.1.7 remains identical |
| `prismlauncher` | `7b81e7c` → `4b35975` | 85 | Develop snapshot changes, including a confirmed optional-filter regression |
| `sops-nix` | `fbf7592` → `13616ff` | 2 | Dependency/vendor-hash refresh; modules and activation implementation unchanged |

## Evidence Coverage And Tool Limitations

- The original lock was preserved before updating. A programmatic comparison verified that every changed direct Git input appeared in `changelog.json`.
- Stable nixpkgs has a complete, locally recovered **419-commit** range (`range_complete = true`). Both the original and repaired-filter artifacts passed uniqueness, matched/unmatched partition, and exact batch-concatenation checks.
- The initial Linearis version assertion prevented Home Manager package evaluation. The script suppressed that error and extracted only **231 packages / 1 match**, rather than failing. All **418 initially unmatched** subjects were reviewed across batches of **100/100/100/100/18**.
- After the pin fix, package extraction was rerun separately, preserving the original evidence: **408 packages / 21 matches / 398 unmatched**, with batches **100/100/100/98**. Every regenerated batch was also reviewed. No grep fallback was used. The script should report individual extraction failures instead of silently accepting a partial package inventory.
- All **21 matched records** and their original/backport PR descriptions were reviewed: 17 touch configured variants, three update unused Discord channels, and one `jwx` record is a regex false positive (`[26.05]` matched `perl5.42.0-wakeonlan`). The package filter omits some profiles, option-injected packages, and dependencies; exhaustive source/configuration review supplies that coverage. Its breaking detector also omits words such as `remove`; the zero-marker count is not a claim that no removal occurred.
- There are **zero `package: BREAKING` markers** in either stable nixpkgs analysis. Relevant removals and migrations were still assessed through the exhaustive scan and upstream reviews.
- `flake-changelog` compares Git `.rev` values and omits changed file inputs. `final-changelog.json` therefore contains an explicitly marked manual `linearis-npm` entry; all eleven final changed inputs are accounted for. Registry metadata has no separate Git commit count.
- GitHub compare data was truncated for stable/unstable nixpkgs, NUR, and PrismLauncher. Stable coverage was recovered locally; NUR's full 388-commit range was recovered through paginated APIs, and PrismLauncher's full 85-commit range through its detached source trees. Unstable review is scoped to actual consumers and their dependency changes.
- The first `direnv allow` invocation was blocked by the harness before execution; it succeeded from `/tmp/opencode`. A server restart interrupted the first corrected build; the resumed build reused completed Nix work. Upstream `.envrc` restrictions prevented some automatic LSP checks; those environments were not approved. Read-only Git/API evidence remained available.
- Non-blocking upstream lookup failures included a GitLab changelog endpoint HTTP 400, GitLab through `gh api` HTTP 410 (recovered with GitLab v4), and missing release/changelog paths. These did not replace commit review with sampling.
- The pinned V2 release-run SHA was no longer fetchable during the V1/V2 migration assessment. Version-matched published V2 code and static installed-binary inspection supplied the migration evidence; live database contents were not queried. Exact V1 sources showed no schema changes between 1.18.28 and 1.18.30.

## Nixpkgs And Closure Package Changes

Versions below come from the actual `am` closure comparison; where multiple versions coexist, a row describes the changed variant rather than claiming all copies were replaced.

| Component | Old | New | Relevance |
|---|---|---|---|
| Linux / initrd | 6.18.49 | 6.18.50 | Default kernel on all hosts; reboot needed to run it |
| linux-firmware | 20260810 | 20260910 | Redistributable device firmware enabled on all hosts |
| Google Chrome | 152.0.7977.82 | 153.0.8010.36 | Work/Yixin browser; major security update |
| Firefox | 155.0 | 155.0.1 | Four managed profiles; CSS-blur hangs, sidebar restoration, profile icons |
| Brave Origin | 1.94.117 | 1.94.121 | Main configured browser from unstable; stable Brave also updates Torag's captive portal |
| Discord | 1.0.138 | 1.0.156 | Directly installed, autostarted, persisted application |
| GitHub CLI | 2.99.0 | 2.100.0 | Development scripts, API access, and Git credential helper |
| runc | 1.4.2 | 1.4.3 | Podman runtime/helper dependency; CVE-2026-41579 fix |
| Claude Code | 2.1.263 | 2.1.269 | Direct flake package |
| OpenCode v1 | 1.18.28 | Removed | Brian requested V2-only after database migration review |
| Linearis | 2026.8.0 | 2026.9.0-next.1 | Source/npm alignment; private attachment downloads |
| PrismLauncher | 2026-09-06 develop | 2026-09-12 develop | Game launcher, with local filter correction |
| curl | 8.21.0 variant | 8.22.0 variant | Dependency refresh; other 8.21.0 copies remain |
| expat | 2.8.3 variant | 2.8.4 variant | XML parsing dependency |
| fontconfig | 2.18.2 | 2.18.3 | Font discovery/rendering dependency |
| gtest | 1.17.0 variant | 1.18.0 variant | Dependency output |
| hwdata | 0.410 | 0.411 | Hardware identifiers |
| imath | 3.2.2 variant | 3.2.3 variant | Image/media dependency |
| libcap-ng | 0.9.3 variant | 0.9.5 variant | Linux capability dependency |
| libde265 | 1.1.1 variant | 1.1.2 variant | Video decoding dependency |
| libmysofa | 1.3.4 variant | 1.3.5 variant | Audio dependency |
| libopenmpt | 0.8.7 | 0.8.9 | Media playback dependency |
| libxml2 | 2.15.3 variant | 2.15.4 variant | XML parsing dependency |
| OpenEXR | 3.4.13 | 3.4.15 | Image dependency |
| OpenSSL | 3.6.3 variant | 3.6.4 variant | TLS dependency |
| PCRE2 | 10.47 | 10.48 | Regular-expression dependency |
| Perl URI | 5.21 | 5.36 | URI parsing; adds MIME-Base32 1.303 |
| publicsuffix-list | 2026-08-14 | 2026-09-02 | Domain boundary data |
| util-linux-minimal | 2.42.2 variant | 2.42.3 variant | Linux utility dependency |
| vid.stab | 1.1.1 snapshot | 1.1.2 | Media dependency |
| x264 | 2025-01-03 snapshot | 2025-09-10 snapshot | Video encoding dependency |

Same-version rebuilds also affect librsvg, sops-install-secrets, systemd, and tinysparql. The V1-only ripgrep 15.2.0 variant leaves the closure; the configured ripgrep remains. GCC NG 15.3.0 appears as an indirect build dependency; it is not a claim that every host compiler/runtime was upgraded.

## Migration And Relevance Review

### Action Required

- **Fixed Linearis source/artifact mismatch.** `modules/home/work.nix` intentionally asserts identical versions. Updating only the source produced `2026.9.0-next.1 != 2026.8.0`. The exact matching npm metadata pin fixes evaluation without weakening the assertion. Its `gitHead` equals `dfe34c2`; both downloaded tarball integrities were checked. Dependencies, executable name, and Node ≥22 requirement are unchanged.
- **Patch PrismLauncher's combined Modrinth filter.** PR [#6045](https://github.com/PrismLauncher/PrismLauncher/pull/6045) omitted `environment:` from one UniversalSide facet. The upstream query was reproduced as HTTP 400; adding the prefix returned HTTP 200. Default, Client-only, and Server-only searches work. Brian approved the temporary fail-on-mismatch substitution in the existing overlay.
- **Remove OpenCode V1 at Brian's request.** Unstable 1.18.30 changes the build channel from `stable` to `prod`, which selects `opencode.db` instead of the existing `opencode-stable.db`. Both filenames exist here and V2 uses canonical `opencode.db`, so upstream's “legacy only when canonical is absent” fallback would not preserve V1 session selection. V2's one-time importer is not bidirectional history synchronization. Instead of adding an explicit V1 database override, Brian chose V2-only: removed the V1 derivation/import and CLI permission entries, and pointed Home Manager's package option at the existing V2-backed `opencode` wrapper. Existing V2 0.0.0-beta-19271 remains pinned.
- **Reboot for kernel/firmware.** Balrog's required reboot passed. Am and Torag still run Linux 6.18.49 and need a reboot to run the deployed 6.18.50 kernel/firmware update.
- **Recover Torag's full boot filesystem.** The first deployment failed while GRUB copied the new initrd. Read-only inspection found zero free bytes on its 500 MiB ESP, about 140 MiB of obsolete systemd-boot kernel/initrd payloads from 2025, and a 31.7 MiB partial copy. The machine was still running the old system; EFI BootCurrent `0003` confirmed GRUB was the active bootloader. Brian approved archiving/removing the legacy entries and payloads and limiting Torag to three GRUB generations. All 24 removed files were archived and individually SHA-256 verified first; none was referenced by current GRUB. Cleanup recovered 171.5 MiB. `configurations/nixos/torag/default.nix` now sets `boot.loader.grub.configurationLimit = lib.mkForce 3`, budgeting for default/rescue initrds and the next update. The selected upstream installer copies new boot files before removing obsolete ones, so lowering the limit alone cannot recover an already-full ESP. Partition sizing was not the cause of an unusually large new initrd: it grew only about 17 KiB.
- **Correct remote health-check invocation.** The prescribed `ssh torag just health` failed with `no justfile found`: SSH starts in `/home/bbrian`, while the recipes live in `/home/bbrian/conf/justfile`. Brian requested fixing both `modules/home/opencode/skills/flake-update/SKILL.md` and `modules/home/opencode/permissions.nix`. The workflow now supplies `--justfile /home/bbrian/conf/justfile` for each remote health command, with matching exact-command permissions. All four remote recipe definitions were checked against the update worktree. They exercise the active system; deployment continues directly from the local update worktree.

### Worth Reviewing

- **Ungoogled Chromium security gap:** the configured desktop/webapp runtime remains **152.0.7977.82** in both updated stable and unstable inputs. The regular Chromium 153 commit does not update it. Chrome 153 fixes 230 security issues, including exploited V8 CVE-2026-87491; this update must not be described as applying those fixes to Ungoogled Chromium.
- **Chrome 153:** XML parsing moves to Rust in several common paths; Privacy Sandbox APIs, Related Website Sets, `document.requestStorageAccessFor`, and nonstandard `_current` navigation are removed/deprecated. No matching configuration migration was identified. Check work login flows, extensions, and webapps after restarting the browser.
- **Discord:** the update adds `DISCORD_USER_DATA_DIR`, module-staging changes, and desktop metadata changes. That variable is unset here, so existing `.config/discord` persistence is still selected. Test voice/video and portal-based screen sharing.
- **PrismLauncher behavior:** optional per-instance latest-Minecraft selection defaults off; “Any” can select snapshots unsupported by loaders and can interact with automatic Java selection. New `PreLoadCommand` and argument splitting before variable expansion can affect persisted custom launch commands. World external tools, resource-version selection, world datapack installation, ATLauncher lifetime/path handling, resource-page responsiveness, and icon scanning also changed. Nested icon directories beyond direct subdirectories are no longer discovered. No declared setting/file migration applies; Qt ≥6.8 is satisfied by the build.
- **Claude Code:** model-dependent Task/Todo availability changed in 2.1.268; unsupported models need `CLAUDE_CODE_ENABLE_TODO_TOOLS=1` if those tools are wanted. Permission checking across symlinks, compounds, `tee`, and negated rules was hardened. `claude auth status --json` now reports `configDirectory`, useful for the work/personal account split. Check tmux/SSH keyboard behavior, session resume, notification hooks, and plugin reloads. Third-party endpoint regressions introduced in intervening releases are fixed by the selected 2.1.269.
- **Runtime/build review:** runc fixes malicious-image `/dev` symlink handling (CVE-2026-41579); Podman normally prefers crun but carries runc. Foundry is the configured OCI workload. The stable curl commit only changes cross-build `wcurl` shebang/runtime-shell handling; native x86_64 builds and binfmt emulation do not exercise the shell-less cross-target path.
- **Brave:** 1.94.121 supplies Chromium 152.0.7977.83, including the preceding .75/.76 and .82/.83 security fixes (notably exploited CVE-2026-85046). It also remains on milestone 152. Profile layout, Origin behavior, and local override arguments are compatible. Nixpkgs renamed its updater to `update.py` but left an unused `passthru.updateScript` reference to `update.sh`; this affects package-maintenance tooling, not this build.

### No Action Needed

- Home Manager changed only its own lock; the followed nixpkgs controls the actual build. Nixvim did not update.
- Nixos-unified's deprecated Darwin alias was fixed upstream; the other two commits only affect upstream CI and ignores. No consumed output/option changed.
- The direct Firefox add-on package list remains empty; extensions use AMO policy URLs. NUR has no core/module changes or repository removals in this range; Stylix's indirect Firefox Color remains identical.
- Sops-nix's module/activation trees are unchanged. Dependency hardening includes gRPC's xDS-server CVE-2026-84445 fix, but that server path is not used by the activation CLI. The previous Balrog persisted-host-key activation race is not fixed by this dependency update.
- No relevant NixOS/Home Manager/Nixvim option removal, service migration, or library API break was found in the exhaustive stable scan. Merge/backport subjects are accounted for without counting their package changes twice. Linux 7.1 removal does not affect the default Linux 6.18 configuration.

### Breaking-Marker Disposition

The stable analyzer emitted no `package: BREAKING` records. The general script's potential-breaking subject preview contained four candidates, all investigated:

| Marker/subject | Disposition |
|---|---|
| Nixos-unified: replace deprecated Darwin alias | Applicable shared code; upstream already fixes it, no local migration |
| `linux_7_1: remove` | Not used; configured default is Linux 6.18 |
| PrismLauncher: pre-declare LinkPair, remove unnecessary header, Windows symlink-copy ordering | Internal/source and Windows behavior; no Linux user-setting migration |
| PrismLauncher: remove unneeded link dry run | Internal implementation cleanup; no removed local option |

Other significant changes were found independently of those markers: the Linearis version mismatch, Prism's malformed API facet, and OpenCode V1's channel/database switch are addressed above.

## Overlay Review

| Overlay | Result | Evidence/removal condition |
|---|---|---|
| `overlays/prismlauncher.nix` | Updated | Keep libVLC, NSS, and Wayland runtime additions: the selected develop wrapper is unchanged and lacks them, even though stock nixpkgs includes Wayland. Add the approved UniversalSide facet correction; remove the substitution once upstream supplies the missing prefix. |
| `overlays/vit.nix` | Comments updated; code kept | VIT 2.3.4/tasklib 2.5.1 still take `taskwarrior2` arguments. Keep remaps until nixpkgs wires them to Taskwarrior 3; independently keep case-insensitive search until upstream/nixpkgs supplies equivalent behavior (VIT issue #208 remains open). |

## Closure Changes

The final **update-branch** `am` closure changes from **6,032 to 6,030 store paths**, replacing **562 with 560**, and decreases from **34,313,305,880 to 34,247,835,616 bytes** (about **62.4 MiB smaller**). The first successful build before V1 removal grew by 128.9 MiB, principally Discord (+109 MiB) and firmware (+16.8 MiB); removing OpenCode V1 and its private ripgrep variant more than offsets that growth. The final skill/permission correction changes configuration store paths and 504 bytes, with no further package-version changes. These figures precede master's separate package removals.

Added: **Perl MIME-Base32 1.303**. Removed: **OpenCode V1 1.18.28**, the **`opencode1` wrapper**, and its private **ripgrep 15.2.0** variant. The `opencode` command now exclusively wraps V2, and another ripgrep variant remains configured. Version replacements and coexisting variants are listed above. Raw old/new closure JSON and added/removed store-path lists are retained as artifacts.

## Deployment And Boot Verification

The final expected systems, including the skill/permission correction, were evaluated from this worktree. At **2026-09-13 07:55 EDT**, all three hosts' `/run/current-system` and `/nix/var/nix/profiles/system` matched them, with **zero failed system units**:

| Host | Expected and deployed system | Running kernel | Result |
|---|---|---|---|
| am | `/nix/store/1j5zkvnczwsgbvlf2w9yx68qkl8ajbhv-nixos-system-am-26.05.20260911.21a67dc` | 6.18.49 | Final September 13 switch passed; desktop reboot pending |
| balrog | `/nix/store/k21qggj8k4b2layn0m25bqi50jdzw16k-nixos-system-balrog-26.05.20260911.21a67dc` | 6.18.50 | Final switch and required September 13 reboot passed |
| torag | `/nix/store/qh9nqrpfq3hvk1p8vqfh4m154hmkm9ds-nixos-system-torag-26.05.20260911.21a67dc` | 6.18.49 | Final September 13 switch passed; desktop reboot pending |

Balrog's final expected system was confirmed immediately before its 07:53 reboot. Its boot ID changed from `4642a97c-e5ef-4763-b04d-434f97fdd9d7` to `a10a383b-1362-4077-9c72-974d906f39e5`; after boot it reported Linux 6.18.50, system state `running`, and zero failed units. The earlier 07:18 reboot also passed, before the final skill/permission correction changed its expected system.

Torag's final GRUB menu contains generations **235/234/233**, references the expected new default and rescue images, and has no missing referenced files. After installation and automatic obsolete-GRUB-file cleanup, `/boot` has **292,159,488 bytes (278.6 MiB) free**. Actual booting of Torag's new generation remains a manual follow-up.

Evidence: `expected-systems-workflow-fix.json`, `deployed-systems-workflow-fix.json`, `balrog-reboot-workflow-fix.json`, `torag-workflow-fix-checks.json`, the checksum manifest/archive, and individual deployment logs under the artifact directory. Earlier intermediate systems and boot checks remain preserved in their original artifacts.

## Verification And Command Record

Commands below run from the update worktree unless their purpose requires an external directory. `W` means `/home/bbrian/Code/conf-update-09-12-26`; `A` means `/tmp/flake-update/update-09-12-26`.

| Command / operation | Result |
|---|---|
| `date +%m-%d-%y`; `git worktree list --porcelain`; `git branch --list update-09-12-26`; initial `git status --short` | 09-12-26; branch/worktree absent; primary clean |
| `git worktree add --no-track -b update-09-12-26 "$W" HEAD` | Created from committed dd50a7b |
| `direnv allow "$W"` | Harness blocked first attempt; succeeded from `/tmp/opencode` before edits |
| Session move to `W` | Completed |
| `flake-update --output-dir "$A" "$W"` | Exit 0; ten Git inputs; complete stable range; initial partial package extraction |
| Python lock/changelog and commit/batch partition checks | Passed; summaries retained in artifacts |
| `ssh -o BatchMode=yes -o ConnectTimeout=10 {balrog,torag} ...` initial hostname/system/kernel/failed-unit checks | Both reachable; kernel 6.18.49; no failed system units |
| `ssh torag keep-awake 24h` | Enabled temporary sleep inhibitor for the workflow |
| Initial local date/hostname/system/kernel/system and user failed-unit queries | am, EDT, Linux 6.18.49; no failed units |
| `nh os build "$W" --out-link "$A/result"` | Failed on Linearis version assertion (`build.log`) |
| `cp -a "$A" /tmp/flake-update/update-09-12-26-initial` | Preserved initial evidence |
| `nix flake update linearis-npm --flake "$W"` | Exact npm pin updated successfully |
| Corrected `nh os build "$W" --out-link "$A/result"` | Interrupted by server restart; resumed successfully (`build-fixed.log`, `build-resumed.log`) |
| `FLAKE_PATH="$W" FLAKE_UPDATE_DIR="$A/refiltered" nixpkgs-changelog 6713828... 21a67dc... --json` | Passed; 408 packages, 21 matches, 398 unmatched; full revisions recorded in JSON |
| `flake-changelog "$A/old-flake.lock" "$W/flake.lock"` | Exit 0; omitted file input; explicitly supplemented and verified final eleven-input accounting |
| Read-only Git/API source, compare, commit, PR, registry-integrity, and release-note queries | Input/overlay/batch findings above; no upstream checkout inside W |
| General and targeted web searches | NixOS 26.05 regressions, Claude 2.1.269, Prism changes, Linux 6.18.50, Chrome 153, Discord screen sharing |
| `nix store diff-closures "$(cat "$A/old-system")" "$A/result"`; `nix path-info --recursive --json` for old/new paths | Captured closure changes and raw JSON; summarized path and package-name differences |
| `nh os test "$W"` | First attempt failed before activation: `Could not acquire lock`, exit 11; later evidence identified concurrent primary-worktree rebuilds |
| `ps ...`; `lslocks`; `sudo -n lslocks ...`; `stat /run/nixos/switch-to-configuration.lock`; `sudo -n flock -n ... true` | No remaining activation process/lock owner found; root lock probe passed; initial holder unknown |
| `nh os test "$W" --verbose --no-nom --diff never --show-activation-logs` | Activated unpatched update; exit 4 from a transient Foundry health unit during container startup |
| `sudo -n podman inspect foundryvtt --format ...`; `systemctl --failed --no-pager` | Foundry subsequently healthy with repeated passing probes; no failed system units; no reset needed |
| Patched `nh os test "$W" --no-nom --show-activation-logs` | Passed; activated patched system mmslhnc… |
| Initial four local `just` checks | All exited 0, but another task subsequently activated old-input system 0jhr7w1… while checks ran; these results do not establish final update health |
| `ps ...`; `readlink /proc/2077558/cwd`; filtered sudo journal | Identified concurrent `nixos-rebuild test --flake .#am --sudo --no-write-lock-file` from `/home/bbrian/conf`; Brian confirmed that task was paused |
| Isolated `nh os test "$W" --no-nom --show-activation-logs` | Re-activated update; again exposed Foundry's startup-health timing failure after the old generation had restarted its container |
| Compiled PrismLauncher facet inspection | Initial UTF-16 probe was inconclusive; UTF-8 inspection found the corrected facet and no malformed facet in the built executable |
| Build collision comparison; `file`; `readlink`; `systemctl show postgresql.service -p ActiveState -p ExecStart` | PostgreSQL service active and explicitly uses plugin wrapper; global CLI collision and X-server doc collisions recorded for baseline reconciliation |
| `nh os build "$W" --out-link "$A/result" --no-nom` after Prism patch and after V1 removal | Both passed |
| `nh os test "$W" --no-nom --show-activation-logs` after V1 removal | Passed; final system rl66p9a… |
| `nix flake check "$W"` after Prism patch and after V1 removal | Both full checks passed, including three host system builds and V2 plugin checks; only the two existing evaluation warnings |
| `nix eval "$W#nixosConfigurations" --json --apply ...` | Saved exact am/balrog/torag output paths and Linux 6.18.50 in `expected-systems.json` |
| Final `just health`; `just vim-health`; `just gnome-check`; `just test-remote-builds` | All passed at 15:08 EDT; expected generation verified before and after each command. Vim warnings match baseline; no failed units; Syncthing two peers; no GNOME packages; 16 build/cache assertions passed |
| `opencode --version`; `opencode2 --version`; PATH lookup for `opencode1` | Both commands report V2 0.0.0-beta-19271; V1 absent |
| `just deploy` | Exit 1: format, full flake checks, devshell deployment, am switch, and balrog switch passed; Torag closure copied but bootloader installation failed with `/boot` ENOSPC (`deploy.log`) |
| September 13 resume probes: hostname, current/default systems, boot IDs, kernels, failed units | Am and Balrog expected systems active/default, zero failed units; am running Linux 6.18.49, Balrog already on 6.18.50; Torag SSH timed out; Brian chose to wait |
| `ssh balrog sudo -n systemctl reboot`; SSH current-system/boot-ID/kernel/failed-unit probes; `systemctl is-system-running --wait` | Passed 2026-09-13 07:18 EDT: expected system verified before reboot, new boot ID afterward, expected system ka5v84q…, Linux 6.18.50, state `running`, zero failed units (`balrog-reboot.json`) |
| Bounded Torag SSH reconnect wait; renewed `ssh torag keep-awake 24h` | Reconnected 2026-09-13 07:20 EDT; sleep inhibitor renewed |
| Torag read-only Python boot-file/generation/space probes; `findmnt`; `bootctl status --no-pager`; EFI-variable inspection; targeted upstream GRUB installer read | Confirmed old system still active, no failed units, full 500 MiB ESP, obsolete 2025 systemd-boot payloads, and copy-before-cleanup ordering. Optional `sudo -n efibootmgr -v` was unavailable; `bootctl` and direct BootCurrent inspection supplied the evidence |
| SSH tar archive, local tar-member/SHA-256 verification, then manifest-checked removal of approved legacy files and partial initrd | 24 files / 179,647,962 payload bytes archived in `torag-legacy-boot-backup.tar` and removed. Removing the empty parent directory initially returned `Directory not empty` because of an empty `.extra-files` subdirectory; follow-up verified every approved file removed and 179,806,208 bytes free. Empty directories were retained |
| `nixpkgs-fmt "$W/configurations/nixos/torag/default.nix"`; final expected-system evaluation | Formatting passed; first parallel all-host evaluation exceeded a 120-second timeout, background retry passed. Am/balrog output paths are unchanged; Torag is now w07z77b… (`expected-systems-final.json`) |
| `nix flake check "$W"` after Torag's generation-limit change | Passed September 13; only the two existing evaluation warnings (`flake-check-torag-cap.log`) |
| `nh os test "$W" -H torag --target-host bbrian@torag --elevation-strategy passwordless --use-substitutes --no-nom --show-activation-logs` | Passed; built and activated Torag system w07z77b… (`torag-test-cap.log`) |
| `nh os switch "$W" -H torag --target-host bbrian@torag --elevation-strategy passwordless --use-substitutes --no-nom --show-activation-logs` | Passed September 13; bootloader installed and expected system w07z77b… active/default (`torag-switch-recovery.log`) |
| Final current/default-system, kernel, boot-ID, and failed-unit probes on all hosts; Torag GRUB reference/capacity check | Passed 07:33 EDT: all expected systems active/default, zero failed units, all new default/rescue boot files present, three Torag menu generations, 278.6 MiB free in `/boot` |
| Original `ssh torag just health` | Failed with `no justfile found` before any health component ran; expected system remained active. Brian requested the skill/permission correction above |
| `ssh torag just --justfile /home/bbrian/conf/justfile --show ...` | Initial multi-recipe `--show` syntax failed; one invocation per recipe passed and verified all four existing remote definitions |
| Final `nh os build "$W" --out-link "$A/result" --no-nom`; `nh os test "$W" --no-nom --show-activation-logs` after the skill/permission correction | Both passed September 13; final am system 1j5zkvn… (`local-workflow-fix-{build,test}.log`) |
| Final `nix flake check "$W"` after the skill/permission correction | Passed; only the two existing evaluation warnings (`flake-check-workflow-fix.log`) |
| Final `just health`; `just vim-health`; `just gnome-check`; `just test-remote-builds` after the skill/permission correction | All passed 07:44 EDT on verified system 1j5zkvn… before/after every recipe; no failed units, two Syncthing peers, existing Vim warnings, no GNOME packages, all 16 build/cache assertions passed |
| Active OpenCode configuration and deployed skill inspection | All four exact remote-check commands present and allowed; superseded wildcard commands absent (`active-workflow-fix.json`) |
| Final `nix path-info --recursive --json`; `nix store diff-closures` | Updated final closure artifacts; no additional package-version changes from the skill/permission correction |
| Final `just deploy` after the skill/permission correction | Passed for am/balrog/torag, including format, full flake checks and devshell deployment (`deploy-workflow-fix.log`) |
| Final expected-system evaluation and all-host active/default-system checks; `ssh balrog sudo -n systemctl reboot` and boot probes | Passed: expected final systems on all hosts; final Balrog reboot completed 07:53 EDT on Linux 6.18.50 with zero failed units |
| `ssh torag just --justfile /home/bbrian/conf/justfile {health,vim-health,gnome-check,test-remote-builds}` | All four passed at 07:53–07:54 EDT with expected generation verified before/after each: zero failed units, two Syncthing peers, existing Vim warnings, no GNOME packages, all 16 build/cache assertions passed |
| Final Git checks, stage, update commit | Changes collected in the update commit containing this report; Brian approved merging into master and subsequent combined-system local checks |

### Newly Recorded Findings

- Foundry's transient startup-health unit can make the first activation return 4 while the container is still `starting`. Later probes and final activation passed without resetting failures. This also occurred in the competing old-input activation and is not established as a new upstream regression.
- PrismLauncher legacy Java compiler warnings, ignored PostgreSQL/X-server documentation collisions, and missing `gawknotes.info` index metadata were observed in local build logs. The build succeeded; PostgreSQL's running service uses its explicit plugin wrapper. These are now tracked in `failures.md` with follow-up rationale.
- Root filesystem usage is **96%**, about **36 GiB free**. No ENOSPC failure occurred. Capacity review is follow-up work.
- Existing intermittent boot/hardware warnings were retained; a quiet recent journal slice does not prove them resolved. Installer ISO build/boot and non-x86_64 checks remain unverified for the updated inputs.
- Torag's boot filesystem exhausted capacity while GRUB copied the new initrd. The failed deployment and subsequent SSH timeout led to the approved legacy-file cleanup and declarative retention limit above; activation and boot-menu verification then passed. This new deployment failure was resolved during the update rather than accepted as an outstanding baseline exception.

### Failure-Baseline Reconciliation

Compared against the fixed starting `failures-before.md`, newly recorded outstanding findings are the transient Foundry startup-health race, legacy Java compiler warnings, package/documentation collisions, info-index metadata, root capacity on am, and update-tooling limitations. Torag now also records the same D-Bus/menu activation noise already tracked on am. Existing evaluation and Vim warnings remain, including Torag's clipboard warning in the SSH-launched headless check. The latter does not establish a GUI clipboard failure.

No starting-baseline finding was removed: quiet journal slices do not exercise the earlier boot/resume hardware conditions. The newly encountered Torag ENOSPC and missing-justfile failures were resolved and verified within this update. Updated installer ISO build/boot, other advertised platforms, GUI-host new-kernel boots, and manual application checks remain unverified.

## Follow-Up And Manual Tests

- Restart browsers. Verify Firefox profiles/custom UI, Chrome work login/extensions, Brave profiles, and persisted webapps; track the outstanding Ungoogled Chromium security update.
- Test Discord voice/video and Wayland screen sharing. Search results included older GNOME/NVIDIA-specific regressions, not proof of a new failure on these hosts.
- In PrismLauncher, test both Modrinth environment boxes together, resource version changes, world datapacks, custom launch-command quoting, icons, Microsoft authentication, Java selection, native Wayland, and WaterMedia/libVLC. Enable automatic snapshot selection only intentionally.
- Check Claude work/personal `configDirectory`, normal-model Task/Todo tools, permission behavior, resume/compaction, notification hooks, plugins, and tmux/SSH keys. New optional output-style, effort-cap, and plugin-evaluation controls require no declarative migration.
- Smoke-test Linearis private attachment download and existing issue workflows; the selected artifact is an exact prerelease, not a moving npm tag.
- Smoke-test normal V2 session/plugin workflows. Both CLI version checks and V1's absence from PATH were verified locally; V2 plugin type/load checks passed as part of `nix flake check`.
- Verify GPU/display, audio/Bluetooth, Wi-Fi, and suspend/resume after GUI hosts boot the new kernel/firmware.
- Fix update tooling's suppressed package-extraction errors and omission of changed non-Git file inputs.

## Post-Merge Verification — September 13

At Brian's request, update commit **`57637eb`** was merged into `master` as **`1957a7c`**, on top of `1068a51` and its five newer commits. The sole conflict was the lock entry for `firefox-addons`: the update advanced it while master removed Firefox and that input. Resolution keeps master's removal and every other updated lock entry. A programmatic comparison verified all 22 non-overlapping master paths and the combined `flake.nix`/lock changes.

Master's Firefox and unused-webapp removals, pinentry reorganization, pixie-boot removal, and new Nix linter are preserved. The upstream review and remote deployment sections above describe the isolated update branch. Post-merge local verification uses the combined `/home/bbrian/conf` configuration; Firefox-specific manual checks above no longer apply to that combined configuration.

| Post-merge command / operation | Result |
|---|---|
| `git status --short --branch`; `git worktree list --porcelain`; `git log -8 --oneline`; targeted master diffs | Primary worktree clean at 1068a51 with five newer commits; preserved those changes |
| `git diff --check`; explicit `git add`; `git commit -m "update flake inputs for 2026-09-12"` | Update committed as 57637eb |
| Session move to `/home/bbrian/conf`; `git merge --no-ff --no-commit update-09-12-26` | One content conflict in flake.lock; resolved by retaining master's Firefox-input removal |
| Programmatic merge-content checks; `git add flake.lock`; `git commit -m "merge flake update 2026-09-12"` | All 22 non-overlapping master paths and combined flake/lock content verified; merge committed as 1957a7c; applicable pre-commit hooks passed |
| `nh os build /home/bbrian/conf --out-link /tmp/flake-update/update-09-12-26/result-merged --no-nom` | Passed |
| `nh os test /home/bbrian/conf --no-nom --show-activation-logs` | Passed; activated merged system snqgzrd… |
| `nix flake check /home/bbrian/conf` | Full pass, including all host check builds and configured lint/plugin checks; only the two existing evaluation warnings |
| `just health`; `just vim-health`; `just gnome-check`; `just test-remote-builds` from `/home/bbrian/conf` | All passed at 08:05 EDT, with expected active generation verified before/after each recipe |
| `nh os switch /home/bbrian/conf --no-nom --show-activation-logs`; final current/default-system and failed-unit probes | Passed; current and default systems both match the tested merged build, with zero failed units at 08:11 EDT |

The combined active/default system on am is `/nix/store/snqgzrdgwrq6yjrnxbl92rw98nnn8dif-nixos-system-am-26.05.20260911.21a67dc`. Health reports zero failed system units, two connected Syncthing peers, root usage 96% with about 36 GiB available, the same tracked Neovim warnings, no GNOME packages, and all 16 build/cache assertions passing. No new warning or failure was found in the post-merge checks. Running Linux remains 6.18.49 pending desktop reboot.

`failures.md` now scopes am and full flake checks to this merged configuration; Balrog/Torag activation evidence retains the verified update-branch systems above. Detailed post-merge evidence is in `merge-verification.json`, `merged-build-test.json`, `merged-local-checks.json`, `merged-final-state.json`, and the `merged-*.log` artifacts.

## Upstream References

- [NixOS 26.05 release notes](https://nixos.org/manual/nixos/stable/release-notes)
- [Linux 6.18.50 changelog](https://cdn.kernel.org/pub/linux/kernel/v6.x/ChangeLog-6.18.50)
- [Chrome 153 release notes](https://developer.chrome.com/release-notes/153) and [security release](https://chromereleases.googleblog.com/2026/09/stable-channel-update-for-desktop_0808145027.html)
- [Claude Code releases](https://github.com/anthropics/claude-code/releases)
- [PrismLauncher Modrinth migration PR #6045](https://github.com/PrismLauncher/PrismLauncher/pull/6045) and [Modrinth facet documentation](https://docs.modrinth.com/api/operations/searchprojects/)
