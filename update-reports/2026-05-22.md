# Flake Update Report - 2026-05-22

Updated `nixpkgs` from `549bd84d6279f9852cae6225e372cc67fb91a4c1` to `d233902339c02a9c334e7e593de68855ad26c4cb`.

## Build Status
- `nh os build` - OK after PrismLauncher fixes
- `nh os test` - OK, activated `/nix/store/yiq2dahd9fj0ncljpi6qwnib5pc34sb3-nixos-system-am-26.05.20260515.d233902`
- `nix flake check` - All checks passed, with existing warnings for unknown flake output `nixos-unified` and deprecated `programs.ssh.matchBlocks`
- `just health` - Failed because Syncthing peer `3MB5CXC` is disconnected; systemd failed units are clean
- `just vim-health` - No errors; warnings for large log, unknown YAML filetypes, and missing `biber`
- `just gnome-check` - Clean after Fastfetch overlay fix
- `just deploy` - Built local/target configs, then failed at `ssh torag git -C ~/conf pull` because `torag` SSH timed out
- `just test-remote-builds` - Failed because `torag` SSH is unavailable and torag cannot reach `am:5000`
- Torag host health checks - Not run; host unreachable over SSH

## Config Changes Required

### PrismLauncher Build Fixes
The upstream PrismLauncher input now fails against current nixpkgs without local compatibility shims.

Changes applied:
- `modules/nixos/default.nix` - added a temporary `extra-cmake-modules = prev.kdePackages.extra-cmake-modules;` overlay shim before `inputs.prismlauncher.overlays.default`
- `overlays/prismlauncher.nix` - added `pkg-config` to `prismlauncher-unwrapped.nativeBuildInputs`

Reason:
- PrismLauncher develop still references the removed top-level `extra-cmake-modules`
- PrismLauncher develop calls `find_package(PkgConfig)` without declaring `pkg-config` in native build inputs

### Hyprland Config Type
Home Manager warned that Hyprland's default config type will change. The repo keeps current behavior explicitly now.

Change applied:
- `modules/home/hyprland/hyprland.nix` - set `wayland.windowManager.hyprland.configType = "hyprlang";`

### Fastfetch GNOME Closure Fix
Fastfetch started pulling in EFL, which pulled in Mint/Ubuntu icon themes and `gnome-icon-theme`, tripping `just gnome-check`.

Change applied:
- `overlays/fastfetch.nix` - override `fastfetch` with `enlightenmentSupport = false`

Result:
- `just gnome-check` is clean
- Local closure removed `efl`, `gnome-icon-theme`, `mint-x-icons`, `ubuntu-themes`, and related packages
- Local system closure decreased by about 337 MiB

### Removed Obsolete Overlay
The local opencode overlay was no longer needed.

Change applied:
- Removed `overlays/opencode.nix`

Reason:
- nixpkgs now provides `opencode` `1.14.48`, newer than the local pin `1.14.46`

## Updated Inputs

| Input | Commits | Notes |
|-------|---------|-------|
| claude-code | 10 | Claude Code package bumps through 2.1.148 |
| disko | 12 | Disk image and test fixes; no config changes required |
| firefox-addons | 19 | Automatic addon index updates; repo has no active package list from this set |
| flake-parts | 2 | Small upstream maintenance changes |
| git-hooks | 4 | pre-commit-hooks.nix maintenance; flake check still passes |
| home-manager | 79 | Includes SSH option deprecation warning now visible in eval |
| nix-darwin | 9 | Not relevant to active NixOS hosts |
| nix-index-database | 2 | Generated database refresh; no active module usage found |
| nixpkgs | 6150 | Main system package/module update |
| nixvim | 46 | NixVim updates; check passes and health has warnings only |
| nur | 397 | NUR churn; import/eval still passes |
| prismlauncher | 13 | Develop branch update; required local build fixes |
| stylix | 5 | Small upstream changes; no config action required |

## nixpkgs Changes Affecting Config

### Action Required
- `extra-cmake-modules` top-level removal broke the PrismLauncher input overlay; fixed with a temporary local overlay shim.
- Fastfetch gained/kept optional Enlightenment support that pulls EFL and GNOME icon themes into the closure; fixed by disabling `enlightenmentSupport`.

### Worth Reviewing
- `postgresql: security patches of 2026-05-14 (#520239)` - config uses PostgreSQL 18 in `modules/nixos/work.nix`.
- `docker: 29.4.1 -> 29.4.2` security and `29.4.2 -> 29.4.3` - config enables Docker in `modules/nixos/docker.nix`.
- `hyprland: 0.55.0 -> 0.55.1` - config has substantial Hyprland usage.
- `tailscale: 1.96.5 -> 1.98.0` - config uses Tailscale service, SSH, and routing features.
- Browser security updates landed for Chromium/Chrome/Firefox-related packages.
- Kernel and microcode security/infrastructure updates landed; normal reboot testing still matters.

### Module/Infrastructure Scan Findings
- `nixos/etc: clear stale opaque markers from mutable overlay upperdir (#507963)` - relevant because this repo uses impermanence and mutable `/etc` behavior matters.
- `nixos/network-interfaces: remove network-setup [v2] (#515773)` - relevant to networking and DHCP/wifi behavior, but no immediate eval breakage was seen.

## Overlay Review
- `overlays/fastfetch.nix` - added; keep while upstream Fastfetch enables Enlightenment support by default and pulls EFL/icon themes.
- `overlays/prismlauncher.nix` - keep; still needed for runtime libraries and now for temporary upstream build fixes.
- `overlays/vit.nix` - keep; nixpkgs still uses `taskwarrior2`, and the case-insensitive search patch is still needed.
- `overlays/taskopen.nix` - keep; nixpkgs still packages `taskopen` `1.1.5`, local overlay wants Nim `2.0.1` taskopen.
- `overlays/opencode.nix` - removed; obsolete because nixpkgs has a newer opencode.

## Health Check Changes from Baseline
- `just gnome-check` is clean again after disabling Fastfetch Enlightenment support.
- The transient Neovim `is not in runtimepath` error did not persist after a rebuild/activation, so no mutable runtimepath workaround was kept.
- Current `just vim-health` warnings are recorded in `failures.md`: large LSP log, unknown YAML filetypes, and missing `biber`.
- `just health` now records Syncthing peer `3MB5CXC` disconnected, likely because `torag` is offline.
- Existing activation-time dbus duplicate-name messages are still present after activation; no failed units were reported.

## Warnings / Follow-up
- Home Manager now warns that `programs.ssh.matchBlocks` is deprecated in `modules/home/work.nix` and `modules/home/ssh.nix`; migrate to `programs.ssh.settings` in a follow-up.
- PrismLauncher still deserves runtime smoke testing because this repo tracks its `develop` branch and the update required build shims.
- `torag` SSH timeout blocked deploy completion, remote build checks, and torag-side health checks.
- Local root filesystem is at 95% used during `just health`; not new to this update, but it is close enough to watch.

## Suggested Tests
1. Launch PrismLauncher and verify normal startup.
2. In PrismLauncher, test Microsoft/Xbox auth and one modpack flow.
3. Bring `torag` online, then rerun `just deploy`, `just test-remote-builds`, `just health`, `just vim-health`, and `just gnome-check` for torag.
4. Reboot `am` when convenient to catch kernel, microcode, networking, and impermanence behavior that activation cannot fully prove.
