# Flake Update Report - 2026-08-04

## Summary

Updated 15 flake inputs. The `am` configuration builds, passes the full flake check, and is switched persistently. The `torag` configuration also builds, but its switch and post-deployment checks were left to the user after it slept during `local-deploy`; remote-build validation was therefore not rerun.

No required NixOS or Home Manager option migrations were found. Local checks match the updated failure baseline, and the GNOME closure is clean.

## Input Updates

| Input | Revision | Highlights |
|---|---|---|
| `nixpkgs` | `0ad6f47` -> `531670d` | 2,451 commits reviewed; security, kernel, browser, networking, and desktop updates |
| `nixpkgs-unstable` | `0bb7ec5` -> `e72e4f2` | Unstable package refresh |
| `home-manager` | `af2beae` -> `d4fd246` | No relevant option changes |
| `claude-code` | `d120e1b` -> `b03bf90` | Claude Code 2.1.206 -> 2.1.221 |
| `meridian` | `01baa0f` -> `8b789e4` | Meridian 1.52.0 -> 1.60.0; service remains healthy |
| `prismlauncher` | `d2fa7cf` -> `d10acdb` | PrismLauncher develop reaches 12.0.0; layout and native Wayland changes |
| `mighty-rearranger` | `c0f5d7e` -> `45829ed` | Added Zulip sync and a DB migration upstream; scheduled service disabled locally |
| `firefox-addons` | `dd7e87f` -> `66f538c` | Extension package refresh |
| `nur` | `bea54b7` -> `4910ad1` | NUR package refresh |
| `stylix` | `e602ad0` -> `bd34c34` | No relevant configuration changes |
| `flake-parts` | `17c9d6c` -> `427bf4b` | No relevant configuration changes |
| `git-hooks` | `bca82ca` -> `43b3c1a` | Hook framework maintenance; checks pass |
| `nix-index-database` | `96d6de1` -> `dbc756c` | Command index refresh |
| `nix-darwin` | `d5bd9cd` -> `15abb8c` | Darwin-only changes; no Linux impact |
| `xlibre-overlay` | `e49386b` -> `3c9798f` | Overlay refresh; existing configuration still evaluates and builds |

## Notable Changes

- Linux 6.18.38 -> 6.18.41 in the new generation. The running kernel remains 6.18.38 until reboot.
- OpenSSH 10.3p1 -> 10.4p1, Tailscale -> 1.98.10, Docker -> 29.6.2, and openresolv -> 3.17.4.
- Chromium/Google Chrome moved to 151.0.7922.71; Firefox is 153.0.1 and Signal Desktop reaches 8.21.0.
- Security fixes include glibc CVE-2026-5435/CVE-2026-6238 and multiple wget CVE patches, plus browser and core-library fixes.
- Xorg server moved to 21.1.24, Xwayland to 24.1.13, and major desktop/core libraries were refreshed.
- PrismLauncher moved `instances/instgroups.json` to `instgroups.json` without migration. The existing file was copied to the new persisted location before activation.
- Mighty Rearranger's new Zulip integration requires the absent Rageveil key `zulip.memorici.de/icfp-announcer-bot/zuliprc`. Since the service had never worked, it and its timer were disabled before their first updated run; the new database migration did not execute.

## Configuration Changes

- Added Wayland to PrismLauncher's runtime libraries for Minecraft 26.1+ native Wayland.
- Excluded only PrismLauncher's flaky `ResourceFolderModel` CTest target after it repeatedly timed out in the Nix sandbox; the other 20 upstream test targets pass.
- Disabled `services.mighty-rearranger` on `am`.
- Explicitly disabled LightDM in the rescue specialization. A July configuration change had unintentionally enabled it after disabling greetd, contradicting the intended TTY-only rescue mode and adding `gnome-themes-extra` to the closure.
- Updated `failures.md` for the current local health baseline.

## Overlay Review

- `overlays/vit.nix` remains necessary because nixpkgs VIT/tasklib still depend on Taskwarrior 2.
- `overlays/prismlauncher.nix` remains necessary for the installed mod runtime libraries. It now also supplies Wayland and carries the targeted flaky-test exclusion.

## Verification

- `nh os build`: passed for `am` and `torag`.
- `nh os test`: passed on `am` after all configuration changes.
- `nix flake check`: passed, including pre-commit and Neovim checks.
- Persistent `am` switch: passed; active generation is `/nix/store/crrdbba32hhwsfz28a1kzyz4isrz7vw9-nixos-system-am-26.05.20260803.531670d`.
- `just health`: no failed units, Syncthing healthy with one peer, root filesystem 82% used.
- `just vim-health`: only known log-size, YAML filetype, and missing `biber` warnings remain.
- `just gnome-check`: clean after removing accidental LightDM from the rescue closure.
- Meridian 1.60.0 health endpoint: healthy and authenticated.
- FoundryVTT container: active and healthy; known license-verification message remains.
- Smoke checks passed for OpenSSH, Tailscale, Firefox, Chromium, OpenCode, Claude Code, Neovim, Taskwarrior, VIT, PrismLauncher, and LibreOffice.
- Torag switch, torag health/Vim/GNOME checks, and `just test-remote-builds`: not completed here; the user continued `local-deploy torag switch` separately.

## Follow-Up

- Reboot `am` when convenient to start Linux 6.18.41 and verify the new generation survives the impermanent-root boot path.
- Confirm torag activation and remote-build checks after its separate deployment completes.
