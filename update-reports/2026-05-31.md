# Flake Update Report - 2026-05-31

Updated `nixpkgs` from `d233902339c02a9c334e7e593de68855ad26c4cb` to `64c08a7ca051951c8eae34e3e3cb1e202fe36786`.

## Build Status
- `flake-update /home/bbrian/conf` - OK; updated 10 top-level inputs and generated changelogs under `/tmp/flake-update/`
- `flake-update --dry-run /home/bbrian/conf` - OK after reboot cleared `/tmp/flake-update`, but dry-run compared against `HEAD~1` for `claude-code`; the report uses the original run's `claude-code` delta from the staged lock diff
- `nh os build` - OK after config fixes; built `/nix/store/xpmzm6rqw37ckvcf66z2ndghziznj59s-nixos-system-am-26.05.20260523.64c08a7`
- `nh os test` - build OK, activation returned exit 4 because the FoundryVTT Podman healthcheck transient unit failed while the container was still `starting`; the next healthcheck reported healthy and `systemctl --failed` was clean
- `nix flake check` - initially failed because updated NixVim treated the release-mismatch warning as a check failure; OK after making NixVim's nixpkgs source and release-check setting explicit
- `just health` - failed only on known Syncthing peer `3MB5CXC` disconnected; journal output also showed the failed activation, one Waybar coredump after activation, and ACPI USB `_PLD` firmware errors
- `just vim-health` - reported the known LSP log/YAML/biber warnings plus a current nvim-treesitter runtimepath warning; baseline updated
- `just gnome-check` - OK, no GNOME packages found
- `just deploy` - built `am` and `torag` configs and ran `nix flake check`, then failed at `ssh torag git -C ~/conf pull` because SSH to `torag` timed out
- `just test-remote-builds` - failed because `torag` SSH is unreachable; `am` nix-serve and signing checks passed before the SSH-dependent checks failed
- Torag health checks - not run; `torag` is unreachable over SSH

## Config Changes Required

### Blueman Option Removal
`services.blueman.withApplet` was removed upstream by the nixpkgs Blueman module revert. Evaluation failed until the obsolete option was removed.

Changes applied:
- `modules/nixos/bt.nix` - removed `services.blueman.withApplet = false;`, kept `services.blueman.enable = true;`

### PrismLauncher Overlay Cleanup
PrismLauncher's `develop` branch now includes the previous local Nix fixes for KF6 ECM and `pkg-config`.

Changes applied:
- `modules/nixos/default.nix` - removed the temporary top-level `extra-cmake-modules` overlay shim
- `overlays/prismlauncher.nix` - removed the temporary `pkg-config` native build input override
- `overlays/prismlauncher.nix` - kept the runtime `libvlc` and `nss` additions for the installed mod set

### NixVim Warning/Check Failure
Updated NixVim now warns when its pinned nixpkgs source differs from the actual followed nixpkgs, and the standalone check treats release-mismatch warnings as failures.

Changes applied:
- `modules/home/nvim/nixvim.nix` - added `nixpkgs.source` plumbing and disabled `version.enableNixpkgsReleaseCheck`
- `modules/home/nvim/default.nix` - passed `flake.inputs.nixpkgs` into the shared NixVim module
- `modules/flake-parts/neovim.nix` - passed `inputs.nixpkgs` into the standalone NixVim check/package module

### Health Baseline
Changes applied:
- `failures.md` - added the current nvim-treesitter runtimepath health warning, ACPI USB `_PLD` firmware errors, and the activation-time Waybar coredump note

## Updated Inputs

| Input | Old | New | Commits | Notes |
|-------|-----|-----|---------|-------|
| `claude-code` | `8d8cd609a31b` | `879750a6fc50` | 2 | Claude Code package bumps to `2.1.156`; no module/config changes |
| `disko` | `65fb947964bd` | `caa775cf67bf` | 13 | LUKS/ZFS/mdadm idempotency and tests; local config uses GPT/LVM/btrfs, not affected |
| `firefox-addons` | `0c2d56c5c5b6` | `ef18b76eabdf` | 8 | Automatic addon index updates; local Firefox addon package list is empty |
| `home-manager` | `928d72376949` | `61e2c9659324` | 13 | Stable version update, translations, new `voxtype` module; no local action |
| `nix-index-database` | `f680e0d3c1db` | `8fba98c80b48` | 2 | Generated database refresh; local config enables HM `programs.nix-index` without importing this input module |
| `nixpkgs` | `d233902339c0` | `64c08a7ca051` | 5496 | Main package/module update; required Blueman fix |
| `nixvim` | `4503b3c5e99c` | `a6e624a04f5f` | 30 | New nixpkgs source/release warnings; required explicit config |
| `nur` | `bcbd99c205dd` | `7b6005e6c813` | 249 | Repository churn, including `hythera` removal; no local `pkgs.nur` or `hythera` usage found |
| `prismlauncher` | `43c11a855578` | `f5d7e76ac486` | 15 | Includes upstream Nix fixes and offline account refresh fix; local temporary shims removed |
| `stylix` | `c1456cc4ba3c` | `d7ba76c960a8` | 2 | HM `useGlobalPkgs`/`autoImport` guard; no local action |

## nixpkgs Changes Affecting Config

| Package / Area | Change | Config Relevance | Action |
|----------------|--------|------------------|--------|
| `blueman` NixOS module | `services.blueman.withApplet` removed | `modules/nixos/bt.nix` set the removed option | Fixed by removing option |
| `hyprland` | `0.55.1 -> 0.55.2`; NixOS module adds `cap_sys_nice`; Lua stubs linked | Primary desktop in `modules/nixos/hyprland.nix` and `modules/home/hyprland/` | Build OK; smoke-test login, portals, window rules |
| `xdg-desktop-portal-hyprland` | Dropped unused systemd dependency | Config explicitly uses Hyprland portal to avoid GNOME portal closure | `just gnome-check` clean |
| `tailscale` | `1.98.0 -> 1.98.2` | Tailscale service, SSH, DNS/routes, exit-node behavior | Verify `tailscale status`, SSH, routes after reboot |
| `docker` / `moby` | `29.4.3 -> 29.5.1` | Docker daemon enabled and user in docker group | Smoke-test `docker ps` and normal dev containers |
| `pipewire` | `1.6.3 -> 1.6.5` | Audio stack and Hyprland mic autostart/default-source setup | Test playback, mic, Bluetooth audio |
| `linux` / `initrd-linux` | `6.18.31 -> 6.18.33` | Default kernel, initrd rollback, hardware modules | Reboot verification required |
| `linux-firmware` | `20260410 -> 20260519` | Wi-Fi/Bluetooth/GPU firmware on both hosts | Reboot and hardware smoke-test |
| `nixos/systemd/initrd` | More accurate `MODULES` check | Custom systemd initrd rollback and `dm-snapshot` modules | No config change; verify next boot |
| `systemd` NixOS patches | Several NixOS-specific patches removed | Declarative services should be fine; imperative unit behavior may differ | No config change; monitor activation/service behavior |
| `nixos/system-environment` | Exports PATH to systemd transient environment | Custom services and transient units | No config change |
| `display-managers` | Absolute PAM module paths | SDDM/autologin/Hyprland session | Verify graphical login after reboot |
| `wpa_supplicant` module | `extraConfigFiles` added to `BindReadOnlyPaths` | Torag Wi-Fi uses wireless secrets file; adjacent hardening change | Test torag Wi-Fi when reachable |
| `firefox` | `150.0.3 -> 151.0.1` | Heavily configured Firefox profiles/policies/extensions | Smoke-test profiles/extensions |
| `ungoogled-chromium` / Chrome | Chromium browser update | Desktop browser and webapp launcher use Chromium | Smoke-test webapps |
| `discord` | OpenASAR module update disabling fix | Installed/autostarted communication app | Launch smoke-test |
| `signal-desktop` | `8.8.0 -> 8.9.1` | Installed communication app | Launch smoke-test |
| `element-desktop` | `1.12.14 -> 1.12.18` | Installed communication app | Launch smoke-test |
| `slack` | `4.48.100 -> 4.49.89` | Installed work app | Launch smoke-test |
| `spotify` | `1.2.84.475 -> 1.2.86.502` | Installed media app with persisted config | Launch smoke-test |
| `prismlauncher` | `10.0-unstable-2026-05-18 -> 2026-05-27` | Installed gaming app with local runtime libs | Build OK; runtime smoke-test and account launch |
| `claude-code` | `2.1.153 -> 2.1.156` | HM uses `inputs.claude-code` native binary | CLI smoke-test |
| `opencode` | `1.14.48 -> 1.15.7` | Installed from nixpkgs after previous local overlay removal | CLI smoke-test if used |
| `nix-direnv` | `3.1.1 -> 3.1.2` | Direnv workflow in this repo | `direnv reload` if shell behavior changes |
| `nix-index-unwrapped` | Structured attrs enabled | HM command-not-found/nix-index integration and persisted cache | Verify `nix-locate` if behavior changes |
| `sops` | `3.13.0 -> 3.13.1` | Secret editing/decryption workflow | Secrets decrypted during build/activation |
| `lvm2` | `2.03.38 -> 2.03.39` | Disk layout uses LVM | Reboot verification |
| `busybox` | CVE-2025-60876 fix | Likely in initrd/minimal tooling | No config action |
| `jq` | CVE patches | Used by local scripts | No config action |
| `curl` | `8.19.0 -> 8.20.0` | Used broadly and by Neovim health | No config action |
| `openssl` / `nss` / `nss-cacert` | Security/library updates | Browsers, TLS, PrismLauncher overlay uses `nss` | No config action |
| `mesa` / `libdrm` / `wayland` | Graphics stack updates | Hyprland and Steam | Test desktop and a Steam game |
| `zathura` | `2026-02-09 -> 2026-05-20` family | PDF viewer installed | Open a PDF if used |

The specialized `/tmp/flake-update/nixpkgs-changelog.json` also flagged these as `BREAKING`, but they are internal package deprecation fixes and no config references were found: `execline: avoid deprecated API in wrapper`, `python3Packages.pytest-flake8: drop deprecated py.path.local hook arg`, `gupnp-tools: Work around xmlIndentTreeOutput deprecation`.

## Overlay Review
- `overlays/fastfetch.nix` - kept; still intentionally disables Enlightenment support to avoid EFL/GNOME icon-theme closure. `just gnome-check` stayed clean.
- `overlays/prismlauncher.nix` - updated; removed obsolete `pkg-config` build shim now fixed upstream, kept `libvlc` and `nss` runtime libs for local mods.
- `overlays/vit.nix` - kept; nixpkgs/tasklib/vit still need the local taskwarrior3 and case-insensitive search behavior.
- `overlays/taskopen.nix` - kept; nixpkgs still packages the older Perl `taskopen`, while this config wants Nim `2.0.1`.
- `modules/nixos/default.nix` PrismLauncher shim - removed; upstream now uses `kdePackages.extra-cmake-modules` directly.

## Closure Changes

Local `am` closure highlights from `nh os build`:
- Added: `etc-environment.d/50-systemd-path.conf`, `security-wrapper-Hyprland-x86_64-unknown-linux-musl`, `tex4ht-jar`
- Removed: `generically`, `noto-fonts-color-emoji-png`
- Size: `22.6 GiB -> 22.7 GiB`, about `+25.3 MiB`

Torag local build diff was produced during `just deploy`, but it compared the built torag system against the currently running local `am` system, so it is not a reliable host-to-host closure delta. The torag config did build successfully.

## Health Check Changes from Baseline
- `just health` has no failed systemd units after the transient Foundry healthcheck recovered.
- `just health` still fails on known Syncthing peer `3MB5CXC` disconnected.
- New journal entries recorded in `failures.md`: activation status 4 from the Foundry healthcheck race, one Waybar coredump after activation, and ACPI USB `_PLD` firmware errors.
- `just vim-health` still has known LSP log, YAML filetype, and `biber` warnings.
- `just vim-health` now also reports the nvim-treesitter local install directory is writable but not in runtimepath; packaged parsers are still loaded from the Nix store.
- `just gnome-check` remains clean.

## Tool / Skill Limitations
- `/tmp/flake-update/config-packages.txt` was empty, so the package-matched nixpkgs analysis was incomplete. I compensated with config searches, closure diff review, and a filtered full nixpkgs commit scan.
- After reboot cleared `/tmp/flake-update`, `flake-update --dry-run` regenerated useful files but compared `claude-code` against `HEAD~1` instead of the pre-update lock in `HEAD`, inflating that input's commit count. The report uses the original run's `8d8cd609 -> 879750a6` delta.
- I do not have a general search-engine tool in this environment. I used GitHub/GitLab commit and PR metadata plus known upstream release/commit subjects instead of broad web search.

## Warnings / Follow-up
- Bring `torag` online or fix SSH reachability, then rerun `just deploy`, `just test-remote-builds`, and torag-side `just health`, `just vim-health`, and `just gnome-check`.
- `nh os test` activation should be watched on the next run because the FoundryVTT Podman healthcheck can briefly fail while the container is still starting. The container recovered and is healthy afterward.
- Home Manager still warns that `programs.ssh.matchBlocks` is deprecated in `modules/home/work.nix` and `modules/home/ssh.nix`; migrate to `programs.ssh.settings` separately.
- Reboot `am` to validate kernel, firmware, LVM/initrd rollback, display-manager PAM, Hyprland capability wrapper, Wi-Fi/Bluetooth, and impermanence behavior.
- Runtime smoke-test PrismLauncher, Firefox profiles/extensions, Chromium webapps, Discord/Signal/Element/Slack, Spotify, Docker containers, Tailscale SSH/routes/DNS, audio/mic/Bluetooth audio, and at least one Steam game.
